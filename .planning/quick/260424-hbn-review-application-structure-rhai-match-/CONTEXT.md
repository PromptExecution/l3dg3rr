# Quick Task Context

Task: review application structure, Rhai match visualization idioms, and consolidate README framing for visual-first bookkeeping ETL graph.

Scope:
- Keep ledgerr centered on bookkeeping and CPA-auditable source-of-truth flows.
- Clarify the application as a strongly typed, ontologically linked, scriptable, visual-first workflow graph for AI/LLM-driven ETL.
- Review Rhai match/switch support and visualization idioms before README edits.
- Link README prominently to GitHub Pages docs.

Review findings:
- Match visualization is already implemented as repeated `match expr => Arm -> target` DSL lines in the Rust mdBook preprocessor and JS live editor.
- Runtime classification Rhai and documentation Rhai DSL are separate surfaces and should be documented separately.
- README was the main consolidation target; code structure already maps naturally to bookkeeping truth, type/domain model, ontology graph, script policy, workflow control, visualization, MCP boundary, and host surfaces.
- `just docgen-check` had a stale static `<svg>` assertion; mdBook emits Mermaid blocks and browser/live-editor JS renders SVG later. Updated the recipe to validate generated Mermaid blocks plus existing link/JS checks.
- Aligned `book/theme/rhai-live-core.js` with its tests and README claims: diagnostics, threshold false-chains, default match arms, match lane metadata, simple rejoin layout, animated SVG movement, and autogenerated glTF data URIs now work in the browser core.
