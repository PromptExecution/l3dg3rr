---
phase: 15-reconciliation-and-commit-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["15-reconciliation-and-commit-guardrails-01"]
files_modified:
  - crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - docs/agent-mcp-runbook.md
  - .planning/phases/15-reconciliation-and-commit-guardrails/15-VALIDATION.md
autonomous: true
requirements: ["RECON-03"]
must_haves:
  truths:
    - "Agent can call explicit reconciliation stage tools over MCP tools/call."
    - "Invariant failures return deterministic blocking diagnostics over MCP transport."
    - "Transport tests prove validate/reconcile/commit guardrails execute end-to-end via stdio MCP."
  artifacts:
    - path: "crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs"
      provides: "Transport-level RECON-03 guardrail verification."
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Adapter parsing and deterministic response shaping for reconciliation tools."
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "tools/list and tools/call dispatch wiring for reconciliation stage tools."
  key_links:
    - from: "tools/list reconciliation entries"
      to: "tools/call reconciliation handlers"
      via: "server dispatch table"
      pattern: "l3dg3rr_(validate|reconcile|commit)"
    - from: "adapter reconciliation error mapping"
      to: "transport result payload"
      via: "isError/error_type/message fields"
      pattern: "isError|error_type|message|blocked_reasons"
---

<objective>
Expose reconciliation guardrails through MCP with deterministic blocking diagnostics and align docs/validation to executable transport checks.

Purpose: satisfy RECON-03 while preserving established passthrough/proxy boundary and deterministic small-model payload semantics.
Output: MCP reconciliation tools, stdio transport e2e tests, and validation/runbook updates.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-CONTEXT.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-01-SUMMARY.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-02-SUMMARY.md
@crates/turbo-mcp/src/mcp_adapter.rs
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@crates/turbo-mcp/tests/mcp_stdio_e2e.rs
@docs/agent-mcp-runbook.md

<decisions_locked>
- D-01: Keep upstream rustledger/docling MCP interfaces unchanged.
- D-02: Reconciliation stage tools are l3dg3rr-owned abstractions.
- D-03: Blocking diagnostics must be deterministic and explicit.
- D-04: Validate/reconcile/commit are explicit enforced stage surfaces.
</decisions_locked>

<interfaces>
From plan 15-01 expected exports:
```rust
pub struct ReconciliationStageRequest { ... }
pub struct ReconciliationStageResponse {
    pub stage: String,
    pub status: String,
    pub blocked_reasons: Vec<String>,
}

impl TurboLedgerService {
    pub fn validate_reconciliation_stage_tool(...);
    pub fn reconcile_reconciliation_stage_tool(...);
    pub fn commit_reconciliation_stage_tool(...);
}
```

From `crates/turbo-mcp/src/mcp_adapter.rs` existing pattern:
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn map_tool_error(error: &ToolError) -> Value;
pub fn unknown_tool_result(tool_name: &str) -> Value;
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED RECON-03 transport checks for deterministic blocking diagnostics over tools/call.
- Task 2 wires adapter/server reconciliation tool catalog and dispatch with explicit deterministic payload contracts.
- Task 3 maps RECON-03 checks into validation/runbook artifacts with concrete executable commands.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED RECON-03 stdio MCP tests for reconciliation stage diagnostics</name>
  <read_first>.planning/phases/15-reconciliation-and-commit-guardrails/15-CONTEXT.md, crates/turbo-mcp/tests/mcp_stdio_e2e.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</read_first>
  <files>crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs</files>
  <behavior>
    - Test 1: `tools/list` includes `l3dg3rr_validate_reconciliation`, `l3dg3rr_reconcile_postings`, and `l3dg3rr_commit_guarded`.
    - Test 2: failing commit path returns deterministic blocking diagnostics (`isError`, `error_type`, stable reason keys).
    - Test 3: passing validate+reconcile path yields explicit commit-allowed deterministic result payload.
  </behavior>
  <action>Create failing subprocess transport tests covering initialize -> notifications/initialized -> tools/list -> tools/call for reconciliation stage tools and deterministic failure semantics per D-03/D-04.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test reconciliation_mcp_e2e -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs` exists with RECON-03 tool coverage.
    - Initial run fails in RED before adapter/server wiring.
  </acceptance_criteria>
  <done>RED transport tests lock deterministic blocking diagnostic behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reconciliation MCP tool catalog and tools/call dispatch wiring</name>
  <read_first>crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs, crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</files>
  <behavior>
    - Reconciliation tool names are advertised deterministically in `tool_catalog`.
    - `tools/call` routes reconciliation stage requests to service-level guardrail APIs from plan 15-01.
    - Blocking diagnostics remain explicit and deterministic in adapter payload shaping.
    - Upstream passthrough/proxy tools remain unchanged.
  </behavior>
  <action>Add adapter parsing/result-shaping helpers and server dispatch branches for reconciliation stage tools, reusing existing deterministic error mapping conventions and preserving upstream MCP interface boundaries (D-01, D-02, D-03).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test reconciliation_mcp_e2e -- --nocapture && cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - RECON-03 transport tests pass.
    - Existing adapter contract tests remain green.
    - `rg -n "l3dg3rr_(validate_reconciliation|reconcile_postings|commit_guarded)" crates/turbo-mcp/src/mcp_adapter.rs crates/turbo-mcp/src/bin/turbo-mcp-server.rs` confirms executable wiring.
  </acceptance_criteria>
  <done>MCP reconciliation stage tools execute with deterministic explicit diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Update runbook and phase validation mapping for reconciliation guardrail transport checks</name>
  <read_first>docs/agent-mcp-runbook.md, .planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md, crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs</read_first>
  <files>docs/agent-mcp-runbook.md, .planning/phases/15-reconciliation-and-commit-guardrails/15-VALIDATION.md</files>
  <action>Create/update phase validation map with RECON-01/02/03 command coverage and add concise runbook guidance for reconciliation stage tool usage and deterministic failure interpretation.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture && cargo test -p turbo-mcp --test reconciliation_mcp_e2e -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `15-VALIDATION.md` contains concrete command mapping for all plan tasks with no `MISSING` entries.
    - `docs/agent-mcp-runbook.md` includes reconciliation MCP tool invocation and blocking-diagnostic guidance.
    - Full turbo-mcp test suite remains green.
  </acceptance_criteria>
  <done>Validation and operator docs align with executable reconciliation transport behavior.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test reconciliation_mcp_e2e -- --nocapture`
- `cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture`
- `cargo test -p turbo-mcp -- --nocapture`
</verification>

<success_criteria>
1. Reconciliation guardrails are callable over MCP via explicit stage tools.
2. Invariant failures produce deterministic explicit blocking diagnostics at transport boundary.
3. Validation and runbook artifacts map directly to executable RECON checks.
</success_criteria>

<output>
After completion, create `.planning/phases/15-reconciliation-and-commit-guardrails/15-02-SUMMARY.md`.
</output>
