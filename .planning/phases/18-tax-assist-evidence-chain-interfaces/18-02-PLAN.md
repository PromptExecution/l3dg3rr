---
phase: 18-tax-assist-evidence-chain-interfaces
plan: 02
type: execute
wave: 2
depends_on: ["18-tax-assist-evidence-chain-interfaces-01"]
files_modified:
  - crates/turbo-mcp/tests/tax_evidence_chain_contract.rs
  - crates/turbo-mcp/src/tax_assist.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["TAXA-02"]
must_haves:
  truths:
    - "Agents can retrieve evidence chains that connect source document, lifecycle events, and reconstructed current state."
    - "Evidence chain payloads are deterministic and concise for small-model use."
    - "Evidence chain responses preserve provenance references for downstream review."
  artifacts:
    - path: "crates/turbo-mcp/tests/tax_evidence_chain_contract.rs"
      provides: "Service-level TAXA-02 contract coverage for source->events->current-state evidence chains."
    - path: "crates/turbo-mcp/src/tax_assist.rs"
      provides: "Evidence-chain assembly logic over ontology + event history + replay projection."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService evidence-chain request/response API."
  key_links:
    - from: "source provenance refs"
      to: "event history records"
      via: "document_ref/tx_id filter normalization"
      pattern: "source|events|filter"
    - from: "event history stream"
      to: "current reconstructed state"
      via: "deterministic replay projection"
      pattern: "reconstructed_state|diagnostics|event_count"
---

<objective>
Implement service-level explainable evidence-chain retrieval for tax decisions using existing ontology and event-sourcing primitives.

Purpose: satisfy TAXA-02 with explicit `source -> events -> current_state` linkage and deterministic output shape.
Output: evidence-chain contracts and implementation wired into TurboLedgerService for MCP transport exposure in the next plan.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tax-assist-evidence-chain-interfaces/18-CONTEXT.md
@.planning/phases/18-tax-assist-evidence-chain-interfaces/18-01-PLAN.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-03-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/tax_assist.rs
@crates/turbo-mcp/src/events.rs
@crates/turbo-mcp/src/ontology.rs

<decisions_locked>
- D-02: Evidence retrieval must be source -> events -> current state.
- D-04: Payloads remain deterministic and concise.
- D-05: Keep MCP boundary and passthrough approach unchanged while adding l3dg3rr-owned surfaces.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/lib.rs`:
```rust
pub fn event_history(&self, filter: EventHistoryFilter) -> Result<EventHistoryResponse, ToolError>;
pub fn replay_lifecycle(&self, request: ReplayLifecycleRequest) -> Result<ReplayLifecycleResponse, ToolError>;
pub fn ontology_query_path_tool(&self, request: OntologyQueryPathRequest) -> Result<OntologyQueryPathResponse, ToolError>;
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED TAXA-02 expected chain payload semantics.
- Task 2 implements deterministic evidence-chain assembly from ontology provenance, filtered lifecycle events, and replayed current state.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED TAXA-02 evidence-chain service contract tests</name>
  <files>crates/turbo-mcp/tests/tax_evidence_chain_contract.rs</files>
  <behavior>
    - Test 1: response contains explicit `source`, `events`, and `current_state` sections.
    - Test 2: event ordering and state fields are deterministic between repeated calls.
    - Test 3: provenance references from source and ambiguity context remain linked in the chain payload.
  </behavior>
  <action>Create failing tests that lock evidence-chain schema and deterministic ordering rules per D-02 and D-04.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_evidence_chain_contract -- --nocapture</automated>
  </verify>
  <done>RED tests fail with explicit TAXA-02 evidence-chain expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic evidence-chain retrieval in TurboLedgerService</name>
  <files>crates/turbo-mcp/src/tax_assist.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - Evidence-chain builder composes ontology source context, lifecycle event history, and replay projection.
    - Filters normalize tx/document references before querying events.
    - Output is deterministic, concise, and preserves provenance and diagnostics.
  </behavior>
  <action>Implement service methods and tax-assist helpers for evidence-chain retrieval by reusing existing ontology/event APIs, avoiding any passthrough contract breakage per D-05.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_evidence_chain_contract -- --nocapture && cargo test -p turbo-mcp --test events_replay_contract -- --nocapture</automated>
  </verify>
  <done>TAXA-02 service behavior passes with explicit source->events->current-state evidence chains.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test tax_evidence_chain_contract -- --nocapture`
- `cargo test -p turbo-mcp --test events_replay_contract -- --nocapture`
</verification>

<success_criteria>
1. Evidence-chain retrieval exposes source, events, and current state in one deterministic payload.
2. Provenance links are preserved across chain sections.
3. Repeated calls with same inputs produce stable ordering and fields.
</success_criteria>

<output>
After completion, create `.planning/phases/18-tax-assist-evidence-chain-interfaces/18-02-SUMMARY.md`.
</output>
