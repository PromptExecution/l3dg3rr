---
phase: 18-tax-assist-evidence-chain-interfaces
plan: 01
type: execute
wave: 1
depends_on: ["17-disintegrate-event-sourced-lifecycle-backbone-03"]
files_modified:
  - crates/turbo-mcp/tests/tax_assist_contract.rs
  - crates/turbo-mcp/src/tax_assist.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["TAXA-01", "TAXA-03"]
must_haves:
  truths:
    - "Tax-assist summaries are produced only from reconciled ontology truth."
    - "Ambiguous tax scenarios are flagged with provenance and explicit review state."
    - "Tax-assist payload sections are deterministic and concise for small-model consumers."
  artifacts:
    - path: "crates/turbo-mcp/tests/tax_assist_contract.rs"
      provides: "Service-level TAXA-01/TAXA-03 contract coverage for deterministic tax outputs and ambiguity flags."
    - path: "crates/turbo-mcp/src/tax_assist.rs"
      provides: "Tax-assist domain logic composed from ontology + reconciliation + event primitives."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService tax-assist request/response interfaces and method wiring."
  key_links:
    - from: "ontology entities/edges"
      to: "tax-assist output rows"
      via: "service composition gated by reconciliation stage outcome"
      pattern: "ontology_path|reconciliation|schedule|fbar"
    - from: "ambiguity detector"
      to: "human review queue"
      via: "deterministic reason keys + provenance refs"
      pattern: "ambiguity|review_state|provenance|reason"
---

<objective>
Define and implement deterministic service-level tax-assist outputs and ambiguity review surfaces built from reconciled ontology truth.

Purpose: satisfy TAXA-01 and TAXA-03 while preserving existing deterministic payload and invariant patterns.
Output: tax-assist service contracts, RED->GREEN tests, and reusable domain module for downstream evidence-chain interfaces.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tax-assist-evidence-chain-interfaces/18-CONTEXT.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-02-SUMMARY.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-02-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/ontology.rs
@crates/turbo-mcp/src/events.rs

<decisions_locked>
- D-01: Tax-assist outputs derive from reconciled ontology truth.
- D-03: Ambiguity includes linked provenance and review state.
- D-04: Payloads remain deterministic and concise.
- D-05: Preserve MCP passthrough boundary by adding l3dg3rr-owned service surfaces only.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/lib.rs`:
```rust
pub fn ontology_query_path_tool(&self, request: OntologyQueryPathRequest) -> Result<OntologyQueryPathResponse, ToolError>;
pub fn reconcile_reconciliation_stage_tool(&self, request: ReconciliationStageRequest) -> Result<ReconciliationStageResponse, ToolError>;
pub fn event_history(&self, filter: EventHistoryFilter) -> Result<EventHistoryResponse, ToolError>;
```

From `crates/turbo-mcp/src/events.rs`:
```rust
pub struct ReplayProjection {
    pub reconstructed_state: String,
    pub event_count: usize,
    pub diagnostics: Vec<String>,
}
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 establishes TAXA request/response contracts used by all downstream tax-assist work (D-04).
- Task 2 defines RED tests for reconciled truth derivation and ambiguity review provenance links (TAXA-01, TAXA-03).
- Task 3 implements deterministic service behavior without changing upstream passthrough surfaces (D-05).
</requirements_traceability>

<tasks>

<task type="auto">
  <name>Task 1: Define tax-assist interface contracts for service composition</name>
  <files>crates/turbo-mcp/src/tax_assist.rs, crates/turbo-mcp/src/lib.rs</files>
  <action>Create tax-assist request/response structs and exported service method signatures for schedule/FBAR outputs and ambiguity review state; keep fields explicit, deterministic, and concise per D-04 and keep boundaries additive per D-05.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test interface -- --nocapture</automated>
  </verify>
  <done>Tax-assist contracts compile and are exported from `lib.rs` for downstream implementation tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED TAXA-01/TAXA-03 service contract tests</name>
  <files>crates/turbo-mcp/tests/tax_assist_contract.rs</files>
  <behavior>
    - Test 1: tax-assist output is blocked or empty unless reconciliation outcome is ready.
    - Test 2: successful output uses ontology-backed identifiers and deterministic ordering for schedule and FBAR evidence rows.
    - Test 3: ambiguity cases include stable `review_state`, `reason`, and provenance references.
  </behavior>
  <action>Create failing contract tests that lock deterministic tax-assist output and ambiguity payload semantics for TAXA-01/TAXA-03 before implementation.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_assist_contract -- --nocapture</automated>
  </verify>
  <done>RED tests fail with clear TAXA-01/TAXA-03 expectations and deterministic payload requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement deterministic tax-assist and ambiguity service logic</name>
  <files>crates/turbo-mcp/src/tax_assist.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - Service derives outputs from ontology entities/edges plus reconciliation gate state.
    - Ambiguity payload exposes stable reason keys, review state, and linked provenance refs.
    - Output ordering is deterministic and concise for small-model agents.
  </behavior>
  <action>Implement tax-assist domain functions and TurboLedgerService wrappers that compose existing ontology, reconciliation, and event primitives while preserving all current invariants and without mutating upstream passthrough interfaces (D-05).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_assist_contract -- --nocapture && cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture</automated>
  </verify>
  <done>TAXA-01 and TAXA-03 service contracts pass with deterministic reconciled ontology outputs and review-ready ambiguity flags.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test tax_assist_contract -- --nocapture`
- `cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture`
</verification>

<success_criteria>
1. Tax-assist outputs are derived from reconciled ontology truth only.
2. Ambiguity records include review state plus linked provenance references.
3. Payload shape/order remains deterministic and concise.
</success_criteria>

<output>
After completion, create `.planning/phases/18-tax-assist-evidence-chain-interfaces/18-01-SUMMARY.md`.
</output>
