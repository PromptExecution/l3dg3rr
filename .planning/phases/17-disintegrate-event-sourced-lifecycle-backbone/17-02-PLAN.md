---
phase: 17-disintegrate-event-sourced-lifecycle-backbone
plan: 02
type: execute
wave: 2
depends_on: ["17-disintegrate-event-sourced-lifecycle-backbone-01"]
files_modified:
  - crates/turbo-mcp/tests/events_replay_contract.rs
  - crates/turbo-mcp/src/events.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["EVT-02"]
must_haves:
  truths:
    - "Entity lifecycle state is reconstructable deterministically from event streams."
    - "Replay output is stable across repeated runs over the same append-only stream."
    - "Reconstruction diagnostics are explicit when stream invariants are broken."
  artifacts:
    - path: "crates/turbo-mcp/tests/events_replay_contract.rs"
      provides: "Executable deterministic replay/reconstruction contracts for EVT-02."
    - path: "crates/turbo-mcp/src/events.rs"
      provides: "Replay projector/reconstructor over append-only lifecycle events."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService replay/reconstruction APIs."
  key_links:
    - from: "event stream input"
      to: "reconstructed entity state"
      via: "deterministic fold/projector"
      pattern: "replay|reconstruct|fold|sequence"
    - from: "stream invariants"
      to: "blocked replay output"
      via: "explicit deterministic diagnostics"
      pattern: "missing_predecessor|sequence_gap|invalid_transition"
---

<objective>
Add deterministic replay/reconstruction capabilities over append-only lifecycle events.

Purpose: satisfy EVT-02 by making event streams a reliable source for rebuilding entity truth.
Output: RED->GREEN replay contract tests, deterministic projector logic, and service replay APIs.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-CONTEXT.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-01-PLAN.md
@crates/turbo-mcp/src/events.rs
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/hsm.rs
@crates/turbo-mcp/tests/events_contract.rs

<decisions_locked>
- D-01: Deterministic replay/reconstruction is in-scope core output.
- D-02: Build on existing service boundaries; no architectural reinvention.
- D-03: Test-first plan execution is mandatory.
- D-04: Replay outputs and diagnostics must be deterministic.
- D-05: Preserve safety/invariants and fail closed on invalid streams.
</decisions_locked>

<interfaces>
Expected from plan 17-01:
```rust
pub struct LifecycleEvent { pub event_id: String, pub sequence: u64, pub event_type: String, ... }
pub trait LifecycleEventStore { fn append(...); fn list(...); }
```

New contracts in this plan:
```rust
pub struct ReplayLifecycleRequest { pub tx_id: Option<String>, pub document_ref: Option<String> }
pub struct ReplayLifecycleResponse { pub reconstructed_state: String, pub event_count: usize, pub diagnostics: Vec<String> }
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED replay determinism contracts for EVT-02 (D-03, D-04).
- Task 2 implements deterministic projector/reconstructor over event streams (D-01, D-05).
- Task 3 adds service replay endpoints with explicit blocked diagnostics and stable outputs (D-02, D-04, D-05).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED EVT-02 contract tests for deterministic replay and reconstruction</name>
  <read_first>.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-CONTEXT.md, crates/turbo-mcp/src/events.rs, crates/turbo-mcp/tests/events_contract.rs</read_first>
  <files>crates/turbo-mcp/tests/events_replay_contract.rs</files>
  <behavior>
    - Test 1: replaying the same ordered stream reconstructs the same entity state across runs (EVT-02, D-04).
    - Test 2: deterministic replay diagnostics are emitted for sequence gaps or invalid lifecycle transitions (D-05).
    - Test 3: replay filtered by tx/document still yields stable, deterministic reconstructed output shape.
  </behavior>
  <action>Create failing replay contract tests covering stream fold determinism, invariant-failure diagnostics (`sequence_gap`, `missing_predecessor`, `invalid_transition`), and stable reconstruction payload fields.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_replay_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/events_replay_contract.rs` exists with EVT-02 coverage.
    - Initial RED run fails before replay implementation is added.
  </acceptance_criteria>
  <done>Failing tests define deterministic replay/reconstruction behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic replay projector and reconstruction contracts</name>
  <read_first>crates/turbo-mcp/tests/events_replay_contract.rs, crates/turbo-mcp/src/events.rs, crates/turbo-mcp/src/hsm.rs</read_first>
  <files>crates/turbo-mcp/src/events.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - Replay projector consumes append-only events in deterministic sequence order.
    - Reconstructor emits stable state tokens and explicit deterministic diagnostics.
    - Filtering hooks support tx/document scoped replay without side effects.
  </behavior>
  <action>Add replay/reconstruction structs and fold logic in `events.rs` using stable sorting and explicit invariant checks; expose contracts in `lib.rs` so service-level APIs can reuse them directly.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_replay_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Replay/reconstruction contracts compile and are exercised by tests.
    - Remaining failures are limited to unfinished service wiring.
  </acceptance_criteria>
  <done>Deterministic replay projector is available for service integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire TurboLedgerService replay/reconstruction APIs with deterministic outputs</name>
  <read_first>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/events.rs, crates/turbo-mcp/tests/events_replay_contract.rs</read_first>
  <files>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/events.rs</files>
  <behavior>
    - Service replay API reconstructs deterministic state from event streams.
    - Invalid streams return explicit blocked diagnostics; no panic or silent fallback.
    - Existing event append and lifecycle tests remain green.
  </behavior>
  <action>Implement service methods for replay/reconstruction by transaction/document scope, enforce deterministic ordering, and return explicit diagnostics with stable field names for small-model consumption.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_contract -- --nocapture && cargo test -p turbo-mcp --test events_replay_contract -- --nocapture && cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `events_replay_contract` passes.
    - `events_contract` and `hsm_resume_contract` remain green after replay wiring.
  </acceptance_criteria>
  <done>Event streams deterministically reconstruct lifecycle state for EVT-02.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test events_replay_contract -- --nocapture`
- `cargo test -p turbo-mcp --test events_contract -- --nocapture`
- `cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture`
</verification>

<success_criteria>
1. Replay/reconstruction is deterministic across repeated runs.
2. Invalid streams fail closed with explicit deterministic diagnostics.
3. Replay APIs integrate cleanly with existing lifecycle/event contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-02-SUMMARY.md`.
</output>
