---
phase: 17-disintegrate-event-sourced-lifecycle-backbone
plan: 01
type: execute
wave: 1
depends_on: ["16-moku-hsm-deterministic-status-and-resume-03"]
files_modified:
  - crates/turbo-mcp/tests/events_contract.rs
  - crates/turbo-mcp/src/events.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["EVT-01"]
must_haves:
  truths:
    - "Lifecycle actions emit append-only typed domain events with no mutation/delete path."
    - "Event payloads for ingest/classification/reconciliation/adjustment actions are deterministic across re-runs."
    - "Service contracts expose explicit event append outcomes for auditability."
  artifacts:
    - path: "crates/turbo-mcp/tests/events_contract.rs"
      provides: "Executable append-only and deterministic event payload contracts for EVT-01."
    - path: "crates/turbo-mcp/src/events.rs"
      provides: "Event type definitions, append-only store primitives, deterministic ID/sequence helpers."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService event append integration across lifecycle operations."
  key_links:
    - from: "lifecycle operation result"
      to: "event append store"
      via: "typed append_event helpers"
      pattern: "append_event|event_type|event_id|sequence"
    - from: "event append path"
      to: "deterministic payload shape"
      via: "stable key/value normalization"
      pattern: "BTreeMap|sorted|deterministic"
---

<objective>
Create the event domain/store foundation with append-only lifecycle event persistence and deterministic payload contracts.

Purpose: satisfy EVT-01 without altering existing passthrough/proxy boundaries (D-02).
Output: RED->GREEN append-only event contract tests plus service-integrated event persistence scaffolding.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-CONTEXT.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-03-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/hsm.rs
@crates/turbo-mcp/tests/hsm_contract.rs
@crates/turbo-mcp/tests/phase4_audit_integrity.rs

<decisions_locked>
- D-01: Scope only append-only events + deterministic reconstruction/query foundation.
- D-02: Extend existing service/MCP boundaries; do not reinvent architecture.
- D-03: Code tasks are test-first.
- D-04: Event payloads and outputs are deterministic and explicit.
- D-05: Preserve invariant/safety posture and append-only semantics.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/lib.rs`:
```rust
pub trait TurboLedger {
    fn ingest_statement_rows(&self, request: IngestStatementRowsRequest) -> Result<IngestStatementRowsResponse, ToolError>;
    fn classify_transaction(&self, request: ClassifyTransactionRequest) -> Result<ClassifyTransactionResponse, ToolError>;
    fn reconcile_excel_classification(&self, request: ReconcileExcelClassificationRequest) -> Result<ReconcileExcelClassificationResponse, ToolError>;
}
```

New contracts created in this plan:
```rust
pub struct LifecycleEvent { /* event_id, sequence, event_type, tx_id, document_ref, occurred_at, payload */ }
pub struct AppendEventResult { pub event_id: String, pub sequence: u64 }
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines failing append-only + deterministic event tests for EVT-01 (D-01, D-03, D-04).
- Task 2 introduces event model/store contracts with append-only invariants (D-01, D-05).
- Task 3 wires lifecycle operations to append events without changing upstream passthrough surfaces (D-02, D-05).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED EVT-01 contract tests for append-only lifecycle events</name>
  <read_first>.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-CONTEXT.md, crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/tests/phase4_audit_integrity.rs</read_first>
  <files>crates/turbo-mcp/tests/events_contract.rs</files>
  <behavior>
    - Test 1: ingest/classify/reconcile/adjust operations append typed events and never mutate prior entries (EVT-01, D-01).
    - Test 2: replaying the same deterministic operation yields stable event payload fields and stable event identity inputs (D-04).
    - Test 3: store API exposes append-only behavior only (no update/delete operations are callable) (D-05).
  </behavior>
  <action>Create failing service-level tests that lock event type vocabulary (`ingest`, `classification`, `reconciliation`, `adjustment`), append-only semantics, and deterministic payload field ordering for lifecycle events.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/events_contract.rs` exists with explicit EVT-01 assertions.
    - Initial run fails in RED before event model/store implementation.
  </acceptance_criteria>
  <done>Failing tests define append-only lifecycle event requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement append-only event domain/store contracts with deterministic payload normalization</name>
  <read_first>crates/turbo-mcp/tests/events_contract.rs, crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/hsm.rs</read_first>
  <files>crates/turbo-mcp/src/events.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - Event domain includes explicit typed lifecycle events with deterministic event ID/sequence fields.
    - Store contract supports append + read only; no update/delete path is exposed.
    - Payload normalization uses deterministic ordering for stable replay/query surfaces.
  </behavior>
  <action>Add `events.rs` with `LifecycleEvent`, event typing enums, deterministic ID/sequence helpers, and append-only store interface; expose module contracts through `lib.rs` with no passthrough boundary changes (D-02).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/src/events.rs` exports append/read contracts and deterministic event fields.
    - Tests compile against new contracts with failures limited to remaining service wiring.
  </acceptance_criteria>
  <done>Event domain/store contracts exist and enforce append-only semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire TurboLedgerService lifecycle operations to append deterministic events</name>
  <read_first>crates/turbo-mcp/src/events.rs, crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/tests/events_contract.rs</read_first>
  <files>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/events.rs</files>
  <behavior>
    - Service appends one typed event per successful lifecycle action in deterministic sequence order.
    - Blocked/failed operations do not mutate prior events and do not violate append-only invariants.
    - Existing classification/audit and HSM contract suites remain green.
  </behavior>
  <action>Integrate append_event calls in service lifecycle paths (`ingest_statement_rows`, `classify_transaction`, `reconcile_excel_classification`, adjustment path) using deterministic timestamp/sequence conventions and explicit `ToolError` boundaries (D-04, D-05).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_contract -- --nocapture && cargo test -p turbo-mcp --test phase4_audit_integrity -- --nocapture && cargo test -p turbo-mcp --test hsm_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `events_contract` tests pass.
    - Existing audit/HSM contracts remain green after event wiring.
  </acceptance_criteria>
  <done>Lifecycle operations persist append-only deterministic events for EVT-01.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test events_contract -- --nocapture`
- `cargo test -p turbo-mcp --test phase4_audit_integrity -- --nocapture`
- `cargo test -p turbo-mcp --test hsm_contract -- --nocapture`
</verification>

<success_criteria>
1. Append-only lifecycle events persist for key operations without mutation/delete paths.
2. Event payload structure and ordering are deterministic.
3. Existing lifecycle/audit contracts remain intact after event integration.
</success_criteria>

<output>
After completion, create `.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-01-SUMMARY.md`.
</output>
