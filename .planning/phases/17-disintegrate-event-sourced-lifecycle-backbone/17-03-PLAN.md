---
phase: 17-disintegrate-event-sourced-lifecycle-backbone
plan: 03
type: execute
wave: 3
depends_on: ["17-disintegrate-event-sourced-lifecycle-backbone-02"]
files_modified:
  - crates/turbo-mcp/tests/events_mcp_e2e.rs
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - docs/agent-mcp-runbook.md
  - .planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-VALIDATION.md
autonomous: true
requirements: ["EVT-03"]
must_haves:
  truths:
    - "Agents can query event history over MCP filtered by transaction/document/time."
    - "Event history responses are deterministic and explicit for small-model processing."
    - "Transport-level blocked/invalid query outcomes are machine-readable and stable."
  artifacts:
    - path: "crates/turbo-mcp/tests/events_mcp_e2e.rs"
      provides: "Transport-level EVT-03 contracts for filtered event-history retrieval."
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Deterministic argument parsing and result shaping for event query tools."
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "tools/list and tools/call dispatch for event replay/history MCP tools."
    - path: ".planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-VALIDATION.md"
      provides: "Nyquist verification mapping for EVT-01/02/03 command coverage."
  key_links:
    - from: "tools/list event entries"
      to: "tools/call filtered history handler"
      via: "adapter constants + server dispatch"
      pattern: "l3dg3rr_event_(replay|history)"
    - from: "history filter request"
      to: "deterministic payload response"
      via: "tx/document/time normalization"
      pattern: "tx_id|document_ref|time_start|time_end|events"
---

<objective>
Expose deterministic filtered event-history retrieval over MCP transport and publish executable verification mapping.

Purpose: satisfy EVT-03 with transaction/document/time filters while preserving existing MCP boundary patterns (D-02).
Output: RED->GREEN MCP e2e contracts, adapter/server event query wiring, runbook + validation artifacts.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-CONTEXT.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-02-PLAN.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-03-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/mcp_adapter.rs
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@crates/turbo-mcp/tests/hsm_mcp_e2e.rs
@docs/agent-mcp-runbook.md

<decisions_locked>
- D-01: Phase scope includes event history filtering by tx/document/time.
- D-02: Reuse current adapter/server MCP boundary patterns.
- D-03: RED->GREEN tests before implementation.
- D-04: Deterministic payload keys/order and explicit fields are required.
</decisions_locked>

<interfaces>
From existing adapter/server patterns:
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn unknown_tool_result(tool_name: &str) -> Value;
```

Target tool contracts:
```rust
const EVENT_REPLAY_TOOL: &str = "l3dg3rr_event_replay";
const EVENT_HISTORY_TOOL: &str = "l3dg3rr_event_history";
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED transport contracts for filtered event-history retrieval (EVT-03, D-03, D-04).
- Task 2 implements adapter/server tool wiring and deterministic query payload shaping (D-02, D-04).
- Task 3 updates runbook and validation mapping so EVT-01/02/03 verification is executable and traceable.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED EVT-03 MCP e2e tests for tx/document/time event-history filters</name>
  <read_first>.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-CONTEXT.md, crates/turbo-mcp/tests/hsm_mcp_e2e.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</read_first>
  <files>crates/turbo-mcp/tests/events_mcp_e2e.rs</files>
  <behavior>
    - Test 1: `tools/list` advertises deterministic event replay/history tool names.
    - Test 2: `tools/call` event-history query accepts `tx_id`, `document_ref`, `time_start`, `time_end` filters and returns deterministic `events` ordering (EVT-03).
    - Test 3: invalid filter combinations return deterministic blocked envelopes with explicit reason keys.
  </behavior>
  <action>Create failing subprocess MCP tests following initialize -> notifications/initialized -> tools/list -> tools/call lifecycle, locking deterministic filtered history payload and blocked query semantics.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_mcp_e2e -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/events_mcp_e2e.rs` exists with EVT-03 transport scenarios.
    - Initial run fails in RED before adapter/server wiring.
  </acceptance_criteria>
  <done>Failing transport tests define deterministic filtered event-query behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MCP adapter/server event replay/history tool wiring with deterministic envelopes</name>
  <read_first>crates/turbo-mcp/tests/events_mcp_e2e.rs, crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</files>
  <behavior>
    - Tool catalog deterministically includes `l3dg3rr_event_replay` and `l3dg3rr_event_history`.
    - Filter parsing normalizes tx/document/time inputs and forwards to service replay/history APIs.
    - Success and blocked outcomes keep explicit deterministic key ordering and reason typing.
  </behavior>
  <action>Add tool constants, filter argument parsers, and deterministic result envelopes in adapter; wire server tools/call branches to event replay/history service methods while preserving existing passthrough/ontology/reconciliation/HSM routes unchanged.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_mcp_e2e -- --nocapture && cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `events_mcp_e2e` passes.
    - Adapter contract tests remain green.
    - `rg -n \"l3dg3rr_event_(replay|history)\" crates/turbo-mcp/src/mcp_adapter.rs crates/turbo-mcp/src/bin/turbo-mcp-server.rs` confirms executable wiring.
  </acceptance_criteria>
  <done>Filtered event replay/history is available over MCP transport for EVT-03.</done>
</task>

<task type="auto">
  <name>Task 3: Publish event query runbook examples and Phase 17 validation map</name>
  <read_first>docs/agent-mcp-runbook.md, crates/turbo-mcp/tests/events_mcp_e2e.rs, .planning/phases/16-moku-hsm-deterministic-status-and-resume/16-VALIDATION.md</read_first>
  <files>docs/agent-mcp-runbook.md, .planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-VALIDATION.md</files>
  <action>Update runbook with deterministic MCP event query examples (tx/document/time filters + blocked reasons), and create `17-VALIDATION.md` mapping all Phase 17 task verify commands to EVT-01/02/03 with no `MISSING` entries.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test events_contract -- --nocapture && cargo test -p turbo-mcp --test events_replay_contract -- --nocapture && cargo test -p turbo-mcp --test events_mcp_e2e -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-VALIDATION.md` exists and maps each task to executable commands.
    - `docs/agent-mcp-runbook.md` includes deterministic event replay/history MCP usage with tx/document/time filters.
  </acceptance_criteria>
  <done>Phase 17 verification and operator guidance are executable and deterministic.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test events_mcp_e2e -- --nocapture`
- `cargo test -p turbo-mcp --test events_replay_contract -- --nocapture`
- `cargo test -p turbo-mcp --test events_contract -- --nocapture`
</verification>

<success_criteria>
1. Event history is queryable over MCP by transaction/document/time filters.
2. Transport payloads are deterministic and explicit for small-model agents.
3. Phase 17 verification mapping is complete and executable.
</success_criteria>

<output>
After completion, create `.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-03-SUMMARY.md`.
</output>
