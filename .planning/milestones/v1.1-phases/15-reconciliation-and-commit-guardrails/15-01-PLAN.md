---
phase: 15-reconciliation-and-commit-guardrails
plan: 01
type: execute
wave: 1
depends_on: ["14-ontology-persistence-and-query-surface-02"]
files_modified:
  - crates/turbo-mcp/tests/reconciliation_contract.rs
  - crates/turbo-mcp/src/reconciliation.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["RECON-01", "RECON-02"]
must_haves:
  truths:
    - "User cannot commit transaction truth when double-entry balancing fails."
    - "User cannot commit transaction truth when source totals/extracted rows/ledger postings disagree."
    - "Validate and reconcile stages return deterministic stage outcomes before commit is allowed."
  artifacts:
    - path: "crates/turbo-mcp/tests/reconciliation_contract.rs"
      provides: "RECON-01/02 executable contract coverage for explicit stage gating."
    - path: "crates/turbo-mcp/src/reconciliation.rs"
      provides: "Deterministic reconciliation and stage guardrail logic."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService reconciliation APIs wired to guardrails."
  key_links:
    - from: "validate stage result"
      to: "reconcile stage execution"
      via: "stage transition gate in service layer"
      pattern: "validate.*reconcile|reconcile.*validate"
    - from: "reconcile stage result"
      to: "commit stage outcome"
      via: "explicit commit blocker evaluation"
      pattern: "commit_blocked|is_balanced|source_totals"
---

<objective>
Implement service-level validate/reconcile/commit guardrails with deterministic failure behavior before any MCP transport expansion.

Purpose: satisfy RECON-01 and RECON-02 while preserving upstream passthrough boundaries and local-first deterministic behavior.
Output: contract tests, reconciliation module, and TurboLedgerService APIs for explicit stage gating.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-CONTEXT.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-02-SUMMARY.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-03-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/tests/interface.rs
@crates/turbo-mcp/tests/phase4_audit_integrity.rs

<decisions_locked>
- D-01: Do not reinvent upstream rustledger/docling MCP interfaces.
- D-02: Implement guardrails as l3dg3rr-owned abstractions layered above upstream capabilities.
- D-03: Emit deterministic explicit blocking diagnostics for invariant failures.
- D-04: Enforce explicit validate -> reconcile -> commit stages.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/lib.rs`:
```rust
pub enum ToolError {
    InvalidInput(String),
    Internal(String),
}

pub trait TurboLedgerTools {
    fn ingest_statement_rows(
        &self,
        request: IngestStatementRowsRequest,
    ) -> Result<IngestStatementRowsResponse, ToolError>;
}

pub struct IngestStatementRowsResponse {
    pub inserted_count: usize,
    pub tx_ids: Vec<String>,
}
```

From `crates/turbo-mcp/src/mcp_adapter.rs`:
```rust
pub fn map_tool_error(error: &ToolError) -> Value;
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED contract tests for RECON-01/02 stage gates and deterministic diagnostics (D-03, D-04).
- Task 2 introduces reconciliation guardrail contracts consumed by implementation and downstream MCP tools (D-02, D-04).
- Task 3 implements service-level stage APIs that block commit unless validate+reconcile pass (D-01, D-02, D-03).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED RECON-01/02 contract tests for validate/reconcile/commit gating</name>
  <read_first>.planning/phases/15-reconciliation-and-commit-guardrails/15-CONTEXT.md, crates/turbo-mcp/tests/interface.rs, crates/turbo-mcp/tests/phase4_audit_integrity.rs</read_first>
  <files>crates/turbo-mcp/tests/reconciliation_contract.rs</files>
  <behavior>
    - Test 1: imbalance in postings returns explicit commit-blocked result (RECON-01, D-03).
    - Test 2: source/extracted/posting total mismatch returns deterministic reconcile failure diagnostics (RECON-02, D-03).
    - Test 3: successful validate+reconcile allows commit-ready outcome with deterministic stage marker (RECON-01/02, D-04).
  </behavior>
  <action>Create failing service-level contract tests that assert explicit stage outputs and stable diagnostic keys/messages for imbalance and totals mismatch cases before implementation.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/reconciliation_contract.rs` exists with explicit RECON-01/02 coverage.
    - Initial run fails in RED before guardrail implementation.
  </acceptance_criteria>
  <done>Failing tests define deterministic stage gating behavior for reconciliation guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define reconciliation stage contracts and deterministic diagnostics module</name>
  <read_first>crates/turbo-mcp/tests/reconciliation_contract.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/src/reconciliation.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - Reconciliation stage types encode explicit `validate`, `reconcile`, and `commit` outcomes (D-04).
    - Diagnostics expose stable machine-readable reason keys for blocked states (D-03).
    - Contracts remain l3dg3rr-owned and do not alter upstream rustledger/docling interface shapes (D-01, D-02).
  </behavior>
  <action>Add reconciliation domain structs/enums and helper evaluators for balancing and totals consistency in a dedicated module; export only the contracts needed by service and later MCP adapter wiring.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/src/reconciliation.rs` defines explicit stage result and diagnostic structures.
    - RED tests compile against these contracts and still fail only on unimplemented behavior.
  </acceptance_criteria>
  <done>Executable reconciliation contracts exist for downstream implementation and MCP exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TurboLedgerService validate/reconcile/commit guardrail APIs</name>
  <read_first>crates/turbo-mcp/src/reconciliation.rs, crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/tests/reconciliation_contract.rs</read_first>
  <files>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/reconciliation.rs</files>
  <behavior>
    - Service returns commit-blocked outcomes when balance or totals invariants fail (RECON-01/02, D-03).
    - Service returns commit-ready outcome only after deterministic validate and reconcile success (D-04).
    - Error handling uses explicit `ToolError::InvalidInput`/`ToolError::Internal` boundaries with no panic-prone shortcuts.
  </behavior>
  <action>Implement reconciliation guardrail execution in TurboLedgerService, using deterministic sorting/aggregation for diagnostics and explicit stage-state transitions; keep upstream passthrough contracts unchanged (D-01) while layering new l3dg3rr abstractions (D-02).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture && cargo test -p turbo-mcp --test interface -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - RECON contract test suite passes.
    - Existing interface suite remains green.
  </acceptance_criteria>
  <done>Service-level reconciliation guardrails are enforced with deterministic diagnostics.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test reconciliation_contract -- --nocapture`
- `cargo test -p turbo-mcp --test interface -- --nocapture`
</verification>

<success_criteria>
1. Validate/reconcile/commit stages exist as explicit service-level guardrails.
2. Commit readiness is blocked unless balancing and source-total checks pass.
3. Failure diagnostics are deterministic and explicit for agent consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/15-reconciliation-and-commit-guardrails/15-01-SUMMARY.md`.
</output>
