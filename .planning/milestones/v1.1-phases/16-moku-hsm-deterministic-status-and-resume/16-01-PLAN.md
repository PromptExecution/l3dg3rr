---
phase: 16-moku-hsm-deterministic-status-and-resume
plan: 01
type: execute
wave: 1
depends_on: ["15-reconciliation-and-commit-guardrails-02"]
files_modified:
  - crates/turbo-mcp/tests/hsm_contract.rs
  - crates/turbo-mcp/src/hsm.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["HSM-01", "HSM-02"]
must_haves:
  truths:
    - "User can execute lifecycle through deterministic HSM states/substates from ingest through summarize."
    - "User cannot perform invalid state transitions; guard failures return explicit deterministic reasons."
    - "Status payload exposes concise deterministic Display hints suitable for small-model agents."
  artifacts:
    - path: "crates/turbo-mcp/tests/hsm_contract.rs"
      provides: "Executable HSM-01/HSM-02 service contracts for lifecycle and guarded transitions."
    - path: "crates/turbo-mcp/src/hsm.rs"
      provides: "HSM state/substate model, transition guards, transition evidence, and deterministic status rendering."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService HSM APIs and tool wrappers."
  key_links:
    - from: "hsm transition request"
      to: "guard evaluation outcome"
      via: "state transition table + invariant checks"
      pattern: "allowed_transitions|guard_reason|transition_evidence"
    - from: "current lifecycle state"
      to: "status display payload"
      via: "deterministic status renderer"
      pattern: "display_state|next_hint|blockers"
---

<objective>
Create the HSM lifecycle domain/service foundation with deterministic states/substates, guard-validated transitions, and small-model Display hints.

Purpose: satisfy HSM-01 and HSM-02 before adding resume persistence and MCP transport exposure.
Output: RED->GREEN contract tests, HSM module, and TurboLedgerService state-transition/status APIs.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-CONTEXT.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-02-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/reconciliation.rs
@crates/turbo-mcp/tests/reconciliation_contract.rs

<decisions_locked>
- D-01: Preserve upstream passthrough/proxy boundaries.
- D-02: Encode deterministic lifecycle states/substates for ingest->normalize->validate->reconcile->commit->summarize.
- D-03: Guard-invalid transitions with explicit deterministic blocked reasons and evidence.
- D-05: Include deterministic Display hint fields for small-model reliability.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/reconciliation.rs`:
```rust
pub struct ReconciliationStageResponse {
    pub stage: String,
    pub status: String,
    pub blocked_reasons: Vec<String>,
    pub stage_marker: String,
    pub diagnostics: Vec<ReconciliationDiagnostic>,
}
```

From `crates/turbo-mcp/src/lib.rs`:
```rust
#[derive(Debug, thiserror::Error)]
pub enum ToolError {
    InvalidInput(String),
    Internal(String),
}
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED lifecycle + guard contracts for HSM-01/HSM-02 (D-02, D-03).
- Task 2 creates HSM state/substate and transition-guard contracts with deterministic display rendering (D-02, D-05).
- Task 3 implements service APIs enforcing guarded transitions and deterministic status fields (D-01, D-03, D-05).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED HSM-01/02 contract tests for lifecycle states and guarded transitions</name>
  <read_first>.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-CONTEXT.md, crates/turbo-mcp/tests/reconciliation_contract.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/tests/hsm_contract.rs</files>
  <behavior>
    - Test 1: lifecycle begins at deterministic ingest root and advances through normalize/validate/reconcile/commit/summarize substates in order (HSM-01, D-02).
    - Test 2: invalid transition attempt returns blocked response with stable `guard_reason` and transition evidence (HSM-02, D-03).
    - Test 3: status response always includes deterministic display hints (`display_state`, `next_hint`, sorted `blockers`) for small models (D-05).
  </behavior>
  <action>Create failing service-level HSM contract tests that lock state/substate vocabulary, guard-denial semantics, and deterministic Display fields before implementation.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/hsm_contract.rs` exists with HSM-01/02 coverage.
    - Initial run fails in RED before module/service implementation.
  </acceptance_criteria>
  <done>Failing tests define deterministic lifecycle and guard requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HSM domain module with deterministic state/substate and guard contracts</name>
  <read_first>crates/turbo-mcp/tests/hsm_contract.rs, crates/turbo-mcp/src/reconciliation.rs</read_first>
  <files>crates/turbo-mcp/src/hsm.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - HSM domain defines explicit state/substate enums and transition request/response structs (D-02).
    - Guard evaluation returns deterministic blocked reason keys and evidence payloads (D-03).
    - Display formatter emits fixed-key status hints with stable vocabulary for agent consumption (D-05).
  </behavior>
  <action>Add `hsm.rs` with lifecycle states, allowed-transition table, guard evaluator, and deterministic status formatter; expose contracts in `lib.rs` without changing existing upstream passthrough interfaces (D-01).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/src/hsm.rs` exports transition/status contracts consumed by tests.
    - Tests compile against these contracts with remaining failures only in unimplemented service wiring.
  </acceptance_criteria>
  <done>HSM contracts exist and are ready for service implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TurboLedgerService lifecycle transition and status APIs</name>
  <read_first>crates/turbo-mcp/src/hsm.rs, crates/turbo-mcp/tests/hsm_contract.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/hsm.rs</files>
  <behavior>
    - Service transition API only allows valid deterministic forward transitions (HSM-01/HSM-02).
    - Invalid transition returns blocked response payload, not panic or silent mutation (D-03).
    - Service status API returns deterministic Display hints (`display_state`, `next_hint`, `resume_hint`, sorted blockers) for small-model agents (D-05).
  </behavior>
  <action>Wire HSM state storage and transition/status methods in TurboLedgerService using explicit guard checks, deterministic ordering of blockers/evidence, and strict error boundaries (`ToolError`) with no panic-prone shortcuts.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_contract -- --nocapture && cargo test -p turbo-mcp --test interface -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `hsm_contract` tests pass.
    - Existing interface tests remain green.
  </acceptance_criteria>
  <done>Service-level lifecycle and guarded transition behavior is deterministic and executable.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test hsm_contract -- --nocapture`
- `cargo test -p turbo-mcp --test interface -- --nocapture`
</verification>

<success_criteria>
1. Lifecycle states/substates are explicitly modeled and executable.
2. Guard-invalid transitions are blocked with deterministic reasons and evidence.
3. Status output includes concise deterministic Display hints for small-model agents.
</success_criteria>

<output>
After completion, create `.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-01-SUMMARY.md`.
</output>
