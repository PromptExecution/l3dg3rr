---
phase: 16-moku-hsm-deterministic-status-and-resume
plan: 02
type: execute
wave: 2
depends_on: ["16-moku-hsm-deterministic-status-and-resume-01"]
files_modified:
  - crates/turbo-mcp/tests/hsm_resume_contract.rs
  - crates/turbo-mcp/src/hsm.rs
  - crates/turbo-mcp/src/lib.rs
autonomous: true
requirements: ["HSM-03"]
must_haves:
  truths:
    - "User can resume an interrupted lifecycle from the last valid checkpoint."
    - "Resume cannot skip required stages or bypass guardrail invariants."
    - "Resume status includes deterministic hint fields that tell small models the next safe action."
  artifacts:
    - path: "crates/turbo-mcp/tests/hsm_resume_contract.rs"
      provides: "Executable resumability contracts for interrupted lifecycle continuation."
    - path: "crates/turbo-mcp/src/hsm.rs"
      provides: "Checkpoint/resume domain logic and deterministic resume marker rendering."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService resume APIs and checkpoint persistence wiring."
  key_links:
    - from: "transition success path"
      to: "checkpoint store"
      via: "last_valid_state_marker persistence"
      pattern: "checkpoint|last_valid|state_marker"
    - from: "resume request"
      to: "guarded transition engine"
      via: "resume target validation"
      pattern: "resume_hint|resume_blocked|allowed_transitions"
---

<objective>
Implement deterministic resume/checkpoint behavior so interrupted lifecycle execution can continue safely from last valid state.

Purpose: satisfy HSM-03 without weakening transition guardrails from plan 16-01.
Output: RED->GREEN resume contracts, HSM checkpoint/resume logic, and service-level resume APIs.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-CONTEXT.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-01-PLAN.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-02-SUMMARY.md
@crates/turbo-mcp/src/hsm.rs
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/tests/hsm_contract.rs

<decisions_locked>
- D-03: Guard semantics must still block invalid transitions.
- D-04: Resume from last valid checkpoint only; no invariant bypass.
- D-05: Deterministic Display hints must remain concise and stable.
</decisions_locked>

<interfaces>
From plan 16-01 expected exports:
```rust
pub struct HsmTransitionRequest { ... }
pub struct HsmTransitionResponse {
    pub state: String,
    pub substate: String,
    pub status: String,
    pub guard_reason: Option<String>,
    pub transition_evidence: Vec<String>,
    pub state_marker: String,
}
```

New contracts to add in this plan:
```rust
pub struct HsmResumeRequest { ... }
pub struct HsmResumeResponse {
    pub resumed: bool,
    pub resume_from: String,
    pub resume_hint: String,
    pub blockers: Vec<String>,
}
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED resume-checkpoint contracts for HSM-03 and deterministic hints.
- Task 2 extends HSM module with last-valid checkpoint persistence and resume guard checks.
- Task 3 wires service APIs to execute deterministic resume without violating lifecycle invariants.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED HSM-03 contract tests for checkpoint and resume behavior</name>
  <read_first>.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-CONTEXT.md, crates/turbo-mcp/tests/hsm_contract.rs, crates/turbo-mcp/src/hsm.rs</read_first>
  <files>crates/turbo-mcp/tests/hsm_resume_contract.rs</files>
  <behavior>
    - Test 1: interrupted lifecycle resumes from exact last valid `state_marker` and proceeds to next legal substate (HSM-03, D-04).
    - Test 2: resume request from unknown/invalid checkpoint is blocked with deterministic reasons (D-03, D-04).
    - Test 3: resume response includes deterministic small-model hints (`resume_from`, `resume_hint`, sorted `blockers`) (D-05).
  </behavior>
  <action>Create failing resume contract tests that lock deterministic checkpoint semantics and explicit resume-blocked diagnostics before implementation.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/hsm_resume_contract.rs` exists with HSM-03 scenarios.
    - Initial run fails in RED before resume implementation.
  </acceptance_criteria>
  <done>Failing tests define deterministic resume requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checkpoint/resume contracts and deterministic resume hints in HSM module</name>
  <read_first>crates/turbo-mcp/tests/hsm_resume_contract.rs, crates/turbo-mcp/src/hsm.rs</read_first>
  <files>crates/turbo-mcp/src/hsm.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - HSM module persists deterministic last-valid checkpoint metadata after successful transitions.
    - Resume evaluator validates checkpoint lineage before allowing continuation.
    - Resume formatter emits deterministic Display hints for small-model agents.
  </behavior>
  <action>Add resume request/response contracts and checkpoint state handlers in `hsm.rs`; ensure blocked outcomes use stable reason keys and sorted blockers (D-03, D-04, D-05).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Resume contracts compile and are consumed by tests.
    - Remaining failures are limited to incomplete service wiring.
  </acceptance_criteria>
  <done>HSM module contains deterministic checkpoint/resume logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire TurboLedgerService resume APIs with invariant-preserving transitions</name>
  <read_first>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/hsm.rs, crates/turbo-mcp/tests/hsm_resume_contract.rs</read_first>
  <files>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/hsm.rs</files>
  <behavior>
    - Service persists last valid state markers during transitions and returns deterministic checkpoint metadata.
    - Resume API re-enters lifecycle from checkpoint only when guard checks pass.
    - Invalid resume never mutates lifecycle state and returns deterministic blocked output.
  </behavior>
  <action>Implement service-level checkpoint persistence and resume methods by reusing guard tables from plan 16-01, preserving deterministic ordering and strict `ToolError` boundaries (D-03/D-04).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_contract -- --nocapture && cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Both `hsm_contract` and `hsm_resume_contract` pass.
    - Resume path preserves deterministic state markers/hints.
  </acceptance_criteria>
  <done>Resumability works from last valid state without invariant regressions.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture`
- `cargo test -p turbo-mcp --test hsm_contract -- --nocapture`
</verification>

<success_criteria>
1. Resume uses last valid checkpoint/state marker only.
2. Resume cannot bypass guard-invalid paths or required lifecycle stages.
3. Resume outputs deterministic hint fields for small-model agent execution.
</success_criteria>

<output>
After completion, create `.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-02-SUMMARY.md`.
</output>
