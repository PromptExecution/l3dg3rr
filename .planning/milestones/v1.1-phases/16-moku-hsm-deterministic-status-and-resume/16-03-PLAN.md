---
phase: 16-moku-hsm-deterministic-status-and-resume
plan: 03
type: execute
wave: 3
depends_on: ["16-moku-hsm-deterministic-status-and-resume-02"]
files_modified:
  - crates/turbo-mcp/tests/hsm_mcp_e2e.rs
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - docs/agent-mcp-runbook.md
  - .planning/phases/16-moku-hsm-deterministic-status-and-resume/16-VALIDATION.md
autonomous: true
requirements: ["HSM-02", "HSM-03"]
must_haves:
  truths:
    - "Agent can run lifecycle transition/status/resume via MCP tools/list and tools/call."
    - "Guard and resume failures are deterministic over transport with explicit machine-readable reasons."
    - "MCP status payloads include concise deterministic Display hints for small-model agents."
  artifacts:
    - path: "crates/turbo-mcp/tests/hsm_mcp_e2e.rs"
      provides: "Transport-level executable checks for HSM transition/status/resume flows."
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Adapter parsing and deterministic JSON shaping for HSM tool responses."
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "tools/list and tools/call dispatch wiring for HSM MCP tools."
    - path: ".planning/phases/16-moku-hsm-deterministic-status-and-resume/16-VALIDATION.md"
      provides: "Nyquist command mapping for HSM-01/02/03 verification."
  key_links:
    - from: "tools/list HSM entries"
      to: "tools/call transition/status/resume handlers"
      via: "server dispatch + adapter tool constants"
      pattern: "l3dg3rr_(hsm_transition|hsm_status|hsm_resume)"
    - from: "service HSM blocked output"
      to: "transport envelope"
      via: "adapter deterministic payload mapping"
      pattern: "isError|error_type|display_state|next_hint|resume_hint|blockers"
---

<objective>
Expose HSM transition/status/resume over MCP transport with deterministic Display hints and verification artifacts aligned to HSM-01/02/03 acceptance.

Purpose: finalize Phase 16 by aligning HSM behavior to existing MCP boundary patterns and small-model deterministic payload expectations.
Output: RED->GREEN MCP e2e tests, adapter/server HSM tool wiring, and validation/runbook updates.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-CONTEXT.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-01-PLAN.md
@.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-02-PLAN.md
@.planning/phases/15-reconciliation-and-commit-guardrails/15-02-SUMMARY.md
@crates/turbo-mcp/src/mcp_adapter.rs
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs
@docs/agent-mcp-runbook.md

<decisions_locked>
- D-01: Upstream passthrough/proxy interfaces remain unchanged.
- D-03: Invalid transition/resume must return deterministic blocked reasons.
- D-05: Transport payload includes deterministic Display hints for small-model agents.
- D-06: Follow established adapter/server MCP boundary patterns from phases 13-15.
</decisions_locked>

<interfaces>
From existing MCP adapter patterns:
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn map_tool_error(error: &ToolError) -> Value;
pub fn unknown_tool_result(tool_name: &str) -> Value;
```

HSM tool contract targets:
```rust
const HSM_TRANSITION_TOOL: &str = "l3dg3rr_hsm_transition";
const HSM_STATUS_TOOL: &str = "l3dg3rr_hsm_status";
const HSM_RESUME_TOOL: &str = "l3dg3rr_hsm_resume";
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED transport checks for deterministic guard/resume behavior and Display hints.
- Task 2 wires HSM MCP tool catalog/dispatch and deterministic adapter payload shaping.
- Task 3 updates runbook and validation map for full HSM requirement verification command traceability.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED HSM MCP e2e tests for transition/status/resume deterministic contracts</name>
  <read_first>.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-CONTEXT.md, crates/turbo-mcp/tests/reconciliation_mcp_e2e.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</read_first>
  <files>crates/turbo-mcp/tests/hsm_mcp_e2e.rs</files>
  <behavior>
    - Test 1: `tools/list` advertises deterministic HSM tool names for transition, status, and resume.
    - Test 2: invalid transition and invalid resume return deterministic blocked payloads with explicit reason keys.
    - Test 3: status/resume payload includes small-model Display hint fields (`display_state`, `next_hint`, `resume_hint`, sorted `blockers`).
  </behavior>
  <action>Create failing subprocess MCP tests using initialize -> notifications/initialized -> tools/list -> tools/call lifecycle, locking deterministic transport-level HSM payload contracts.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_mcp_e2e -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/hsm_mcp_e2e.rs` exists with HSM transport scenarios.
    - Initial run fails in RED before adapter/server HSM wiring.
  </acceptance_criteria>
  <done>RED transport tests define HSM deterministic MCP contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement HSM MCP adapter parsing/payload shaping and server dispatch</name>
  <read_first>crates/turbo-mcp/tests/hsm_mcp_e2e.rs, crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</files>
  <behavior>
    - HSM tools are included deterministically in `tool_catalog`.
    - `tools/call` dispatch routes HSM requests to service APIs from plans 16-01/16-02.
    - Adapter output preserves deterministic Display hint fields and stable blocked diagnostics.
    - Existing passthrough/reconciliation/ontology tools remain intact.
  </behavior>
  <action>Add HSM tool constants, argument parsers, and deterministic result envelopes in adapter, then wire server dispatch branches following established phase 13-15 MCP boundary pattern (D-06).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_mcp_e2e -- --nocapture && cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - HSM MCP e2e tests pass.
    - Adapter contract tests remain green.
    - `rg -n "l3dg3rr_hsm_(transition|status|resume)" crates/turbo-mcp/src/mcp_adapter.rs crates/turbo-mcp/src/bin/turbo-mcp-server.rs` confirms executable wiring.
  </acceptance_criteria>
  <done>HSM transition/status/resume are callable over deterministic MCP transport.</done>
</task>

<task type="auto">
  <name>Task 3: Publish HSM validation map and runbook guidance with deterministic Display interpretation</name>
  <read_first>docs/agent-mcp-runbook.md, crates/turbo-mcp/tests/hsm_mcp_e2e.rs, .planning/phases/15-reconciliation-and-commit-guardrails/15-VALIDATION.md</read_first>
  <files>docs/agent-mcp-runbook.md, .planning/phases/16-moku-hsm-deterministic-status-and-resume/16-VALIDATION.md</files>
  <action>Update runbook with HSM MCP lifecycle examples and deterministic Display-hint interpretation guidance; create `16-VALIDATION.md` mapping all Phase 16 task verify commands to HSM-01/02/03 with no `MISSING` entries.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test hsm_contract -- --nocapture && cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture && cargo test -p turbo-mcp --test hsm_mcp_e2e -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `16-VALIDATION.md` exists and maps each plan task to executable commands.
    - `docs/agent-mcp-runbook.md` documents deterministic HSM status/resume interpretation for small-model agents.
    - Full turbo-mcp suite stays green.
  </acceptance_criteria>
  <done>Phase 16 verification and operator guidance are executable and deterministic.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test hsm_mcp_e2e -- --nocapture`
- `cargo test -p turbo-mcp --test hsm_resume_contract -- --nocapture`
- `cargo test -p turbo-mcp --test hsm_contract -- --nocapture`
</verification>

<success_criteria>
1. HSM transition/status/resume tools execute over MCP transport.
2. Guard and resume blocked outcomes are deterministic and machine-readable.
3. Small-model Display hints are explicit, stable, and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/16-moku-hsm-deterministic-status-and-resume/16-03-SUMMARY.md`.
</output>
