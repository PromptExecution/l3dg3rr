---
phase: 18-tax-assist-evidence-chain-interfaces
plan: 03
type: execute
wave: 3
depends_on:
  - "18-tax-assist-evidence-chain-interfaces-01"
  - "18-tax-assist-evidence-chain-interfaces-02"
files_modified:
  - crates/turbo-mcp/tests/tax_assist_mcp_e2e.rs
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - docs/agent-mcp-runbook.md
  - .planning/phases/18-tax-assist-evidence-chain-interfaces/18-VALIDATION.md
autonomous: true
requirements: ["TAXA-01", "TAXA-02", "TAXA-03"]
must_haves:
  truths:
    - "Agents can call tax-assist and evidence-chain tools via MCP tools/list and tools/call."
    - "Tax-assist MCP responses remain deterministic, concise, and machine-readable for small-model execution."
    - "Ambiguity review payloads include linked provenance and explicit review state over transport."
  artifacts:
    - path: "crates/turbo-mcp/tests/tax_assist_mcp_e2e.rs"
      provides: "Transport-level TAXA MCP contracts for list/call behavior and deterministic envelopes."
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Tax-assist tool constants, argument parsers, and deterministic response shaping."
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "tools/call dispatch routing for tax-assist MCP tools."
    - path: ".planning/phases/18-tax-assist-evidence-chain-interfaces/18-VALIDATION.md"
      provides: "Nyquist command mapping for TAXA-01/02/03 verification."
  key_links:
    - from: "tools/list tax-assist entries"
      to: "tools/call tax-assist handlers"
      via: "adapter constants + server dispatch branches"
      pattern: "l3dg3rr_tax_(assist|evidence_chain|ambiguity_review)"
    - from: "service tax-assist responses"
      to: "MCP payload"
      via: "deterministic explicit envelope mapping"
      pattern: "summary|source|events|current_state|ambiguity|review_state"
---

<objective>
Expose tax-assist and evidence-chain interfaces over MCP transport with deterministic envelopes and verification/runbook alignment.

Purpose: close TAXA transport delivery while preserving current MCP passthrough and tool dispatch architecture.
Output: RED->GREEN MCP e2e tests, adapter/server wiring, and Phase 18 validation artifacts.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tax-assist-evidence-chain-interfaces/18-CONTEXT.md
@.planning/phases/18-tax-assist-evidence-chain-interfaces/18-01-PLAN.md
@.planning/phases/18-tax-assist-evidence-chain-interfaces/18-02-PLAN.md
@.planning/phases/17-disintegrate-event-sourced-lifecycle-backbone/17-03-SUMMARY.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/src/mcp_adapter.rs
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@docs/agent-mcp-runbook.md

<decisions_locked>
- D-04: Deterministic, concise payloads for small models.
- D-05: Preserve MCP boundary and upstream passthrough approach.
- D-03: Ambiguity review payload carries provenance and review state.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/mcp_adapter.rs`:
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn unknown_tool_result(tool_name: &str) -> Value;
pub fn map_tool_error(error: &ToolError) -> Value;
```

Transport pattern from `crates/turbo-mcp/src/bin/turbo-mcp-server.rs`:
```rust
match method {
  "tools/list" => { ... }
  "tools/call" => { ... }
}
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED transport-level TAXA behavior for tool listing/calls and deterministic envelope shape.
- Task 2 implements adapter/server MCP wiring preserving passthrough boundaries.
- Task 3 publishes executable validation and runbook usage guidance tied to TAXA requirements.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED TAXA MCP e2e tests for tax-assist, evidence-chain, and ambiguity review tools</name>
  <files>crates/turbo-mcp/tests/tax_assist_mcp_e2e.rs</files>
  <behavior>
    - Test 1: `tools/list` advertises deterministic tax-assist tool names.
    - Test 2: `tools/call` tax-assist/evidence-chain responses include explicit deterministic sections and concise payload keys.
    - Test 3: ambiguity review transport payload includes provenance references and review state fields.
  </behavior>
  <action>Create failing subprocess MCP tests following initialize -> notifications/initialized -> tools/list -> tools/call lifecycle and lock payload semantics for TAXA-01/02/03.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_assist_mcp_e2e -- --nocapture</automated>
  </verify>
  <done>RED transport tests fail with explicit TAXA MCP contract expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MCP adapter and server wiring for tax-assist transport interfaces</name>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</files>
  <behavior>
    - Tool catalog includes tax-assist tool constants in deterministic order.
    - Arguments are parsed and normalized through adapter-owned parsing functions.
    - Success and blocked outcomes use explicit deterministic envelopes with stable key vocab.
    - Existing passthrough and previously shipped tools remain behaviorally unchanged.
  </behavior>
  <action>Add tax-assist constants, parsing, and transport result shaping in adapter, then route tools/call dispatch branches in server using established phase 13-17 boundary pattern (D-05).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_assist_mcp_e2e -- --nocapture && cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <done>TAXA service surfaces are callable over MCP with deterministic concise payload contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Update runbook and phase validation mapping for TAXA verification coverage</name>
  <files>docs/agent-mcp-runbook.md, .planning/phases/18-tax-assist-evidence-chain-interfaces/18-VALIDATION.md</files>
  <action>Document tax-assist MCP usage examples and ambiguity review interpretation, then create `18-VALIDATION.md` mapping all phase task verify commands to TAXA-01/02/03 with no `MISSING` placeholders.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test tax_assist_contract -- --nocapture && cargo test -p turbo-mcp --test tax_evidence_chain_contract -- --nocapture && cargo test -p turbo-mcp --test tax_assist_mcp_e2e -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <done>Phase 18 verification and operator guidance are executable, deterministic, and requirement-complete.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test tax_assist_mcp_e2e -- --nocapture`
- `cargo test -p turbo-mcp --test tax_evidence_chain_contract -- --nocapture`
- `cargo test -p turbo-mcp --test tax_assist_contract -- --nocapture`
</verification>

<success_criteria>
1. MCP tools expose tax-assist outputs and evidence chains without changing passthrough behavior.
2. Transport payloads are deterministic, concise, and small-model friendly.
3. Ambiguity review state and provenance links are available end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/18-tax-assist-evidence-chain-interfaces/18-03-SUMMARY.md`.
</output>
