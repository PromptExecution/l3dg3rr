---
phase: 14-ontology-persistence-and-query-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/turbo-mcp/src/ontology.rs
  - crates/turbo-mcp/src/lib.rs
  - crates/turbo-mcp/tests/ontology_contract.rs
autonomous: true
requirements: ["ONTO-01", "ONTO-02"]
must_haves:
  truths:
    - "Ontology entities and evidence relations persist with referential integrity."
    - "Invalid edges that reference missing entities are rejected deterministically."
    - "Agents can query evidence-chain relationships from document to transaction context."
  artifacts:
    - path: "crates/turbo-mcp/src/ontology.rs"
      provides: "Ontology entity/edge model, local persistence, deterministic traversal/query primitives."
    - path: "crates/turbo-mcp/src/lib.rs"
      provides: "TurboLedgerService ontology APIs owned by l3dg3rr abstractions."
    - path: "crates/turbo-mcp/tests/ontology_contract.rs"
      provides: "Requirement-tagged ONTO-01/02 contract coverage."
  key_links:
    - from: "TurboLedgerService ontology upsert/query methods"
      to: "local ontology store"
      via: "content-hash IDs and referential checks"
      pattern: "upsert|query|missing_ref"
    - from: "ontology_contract tests"
      to: "ontology persistence/query implementation"
      via: "cargo test ontology_contract"
      pattern: "onto_01|onto_02"
---

<objective>
Implement the ontology domain foundation in l3dg3rr with deterministic local persistence and relationship query primitives.

Purpose: satisfy ONTO-01/02 without changing upstream rustledger/docling passthrough interfaces.
Output: ontology model/store + service methods + ONTO-01/02 contract tests.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-CONTEXT.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-RESEARCH.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-03-SUMMARY.md
@crates/turbo-mcp/src/lib.rs

<decisions_locked>
- D-01: Keep upstream rustledger/docling MCP passthrough pattern; do not reinvent those interfaces.
- D-02: l3dg3rr owns higher-level ontology abstractions.
- D-03: Outputs must be deterministic, concise, and machine-readable for small-model agents.
- D-04: Persistence remains local-first and git-compatible.
</decisions_locked>

<interfaces>
From crates/turbo-mcp/src/lib.rs:
```rust
pub trait TurboLedgerTools {
    fn ingest_statement_rows(
        &self,
        request: IngestStatementRowsRequest,
    ) -> Result<IngestStatementRowsResponse, ToolError>;
    fn ingest_pdf(&self, request: IngestPdfRequest) -> Result<IngestPdfResponse, ToolError>;
}
```

From crates/turbo-mcp/src/mcp_adapter.rs (boundary to preserve):
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn normalize_rows_with_provenance(...) -> Vec<Value>;
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines failing tests for ONTO-01/02 behavior and deterministic ordering (per D-03).
- Task 2 implements ontology entity/edge persistence with referential integrity and local storage (per D-02, D-04).
- Task 3 wires service-owned ontology query methods while keeping upstream passthrough interfaces unchanged (per D-01, D-02).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ONTO-01/02 RED contract tests for ontology persistence and relationship query</name>
  <read_first>.planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md, crates/turbo-mcp/tests/mcp_adapter_contract.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/tests/ontology_contract.rs</files>
  <behavior>
    - Test 1: valid ontology entities + edges persist and can be reloaded with stable IDs (ONTO-01).
    - Test 2: edge upsert fails with deterministic invalid-input error when `from`/`to` reference missing entities (ONTO-01).
    - Test 3: query traversal returns ordered path from document -> transaction -> tax/evidence nodes (ONTO-02).
  </behavior>
  <action>Add requirement-tagged failing tests in `ontology_contract.rs` for ONTO-01/02 using deterministic expectations and concise machine-readable assertions per D-03.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test ontology_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/ontology_contract.rs` exists with ONTO-01 and ONTO-02 test names.
    - First run of `cargo test -p turbo-mcp --test ontology_contract -- --nocapture` fails in RED before implementation.
  </acceptance_criteria>
  <done>Failing tests explicitly define required ontology persistence and query behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement local ontology model/store with referential integrity and deterministic ordering</name>
  <read_first>crates/turbo-mcp/tests/ontology_contract.rs, .planning/phases/14-ontology-persistence-and-query-surface/14-RESEARCH.md, AGENTS.md</read_first>
  <files>crates/turbo-mcp/src/ontology.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - Entities (document/account/institution/transaction/tax-category/evidence-reference) upsert deterministically with content-hash IDs.
    - Edge inserts enforce `from`/`to` existence and return explicit deterministic errors when invalid.
    - Store format remains local-first git-compatible (JSON/JSONL) and orders entities/edges stably.
  </behavior>
  <action>Create a new ontology module owned by l3dg3rr abstractions (D-02) with local file-backed persistence (D-04), deterministic entity/edge ordering (D-03), and strict referential checks for ONTO-01. Do not alter upstream rustledger/docling APIs (D-01).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test ontology_contract onto_01 -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp --test ontology_contract onto_01 -- --nocapture` passes.
    - `rg -n "Ontology(Entity|Edge|Store)|missing_ref|content_hash" crates/turbo-mcp/src/ontology.rs` shows model/invariant coverage.
  </acceptance_criteria>
  <done>Ontology entities/edges persist locally with deterministic IDs and referential integrity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ontology relationship query APIs on TurboLedgerService</name>
  <read_first>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/ontology.rs, crates/turbo-mcp/tests/ontology_contract.rs</read_first>
  <files>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/ontology.rs</files>
  <behavior>
    - Service exposes ontology upsert/query methods with concise deterministic response shapes.
    - Relationship traversal supports document -> extracted transaction -> evidence/tax nodes for ONTO-02.
    - Query results use stable ordering to avoid small-model ambiguity (D-03).
  </behavior>
  <action>Wire ontology store/query functions into TurboLedgerService-owned APIs (D-02), keeping MCP passthrough surfaces unchanged (D-01), and pass ONTO-02 traversal tests with deterministic ordering (D-03).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test ontology_contract -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp --test ontology_contract -- --nocapture` passes end-to-end.
    - Existing turbo-mcp test suite remains green.
  </acceptance_criteria>
  <done>Ontology persistence and relationship-query core is implemented and regression-safe.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test ontology_contract -- --nocapture`
- `cargo test -p turbo-mcp -- --nocapture`
</verification>

<acceptance_criteria>
- ONTO-01: ontology entities/edges persist locally with referential integrity and deterministic identifiers.
- ONTO-02: relationship query traverses evidence-chain links in deterministic ordered output.
- Upstream rustledger/docling passthrough interfaces are unchanged.
</acceptance_criteria>

<success_criteria>
1. l3dg3rr owns ontology abstraction/state with strict invariant checks.
2. Deterministic ONTO-01/02 behavior is proven by automated tests.
3. Phase 13 MCP boundary patterns remain intact for downstream tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ontology-persistence-and-query-surface/14-01-SUMMARY.md`.
</output>
