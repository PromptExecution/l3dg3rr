---
phase: 14-ontology-persistence-and-query-surface
plan: 02
type: execute
wave: 2
depends_on: ["14-ontology-persistence-and-query-surface-01"]
files_modified:
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - crates/turbo-mcp/tests/ontology_mcp_e2e.rs
  - docs/agent-mcp-runbook.md
  - .planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md
autonomous: true
requirements: ["ONTO-03"]
must_haves:
  truths:
    - "Ontology query/export is callable over MCP tools/call with deterministic concise payloads."
    - "Serialized ontology snapshots are stable across reruns for the same inputs."
    - "Agent-facing responses preserve explicit provenance links for evidence-chain reasoning."
  artifacts:
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Ontology MCP tool argument parsing and deterministic result shaping."
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "tools/list and tools/call wiring for ontology MCP surface."
    - path: "crates/turbo-mcp/tests/ontology_mcp_e2e.rs"
      provides: "Transport-level ONTO-03 verification through stdio MCP lifecycle."
  key_links:
    - from: "tools/list ontology entries"
      to: "tools/call ontology handlers"
      via: "server dispatch"
      pattern: "ontology_.*"
    - from: "ontology export handler"
      to: "ontology store snapshot"
      via: "deterministic serialization ordering"
      pattern: "entities|edges|snapshot"
---

<objective>
Expose ontology query and deterministic serialization for agent consumption through the established MCP boundary.

Purpose: satisfy ONTO-03 while preserving existing upstream passthrough pattern and small-model reliability constraints.
Output: ontology MCP tools + transport e2e tests + runbook/validation alignment.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-CONTEXT.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-RESEARCH.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md
@.planning/phases/14-ontology-persistence-and-query-surface/14-01-SUMMARY.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-03-SUMMARY.md
@crates/turbo-mcp/src/mcp_adapter.rs
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@crates/turbo-mcp/tests/mcp_stdio_e2e.rs

<decisions_locked>
- D-01: Keep upstream rustledger/docling MCP passthrough pattern; do not reinvent those interfaces.
- D-02: l3dg3rr owns higher-level ontology abstractions.
- D-03: Outputs must be deterministic, concise, and machine-readable for small-model agents.
- D-04: Persistence remains local-first and git-compatible.
</decisions_locked>

<interfaces>
From plan 14-01 outputs:
```rust
// expected service-level ontology methods exposed in lib.rs
pub fn ontology_upsert_entities(...);
pub fn ontology_upsert_edges(...);
pub fn ontology_query_path(...);
pub fn ontology_export_snapshot(...);
```

From crates/turbo-mcp/src/mcp_adapter.rs:
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn map_tool_error(error: &ToolError) -> Value;
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 defines RED transport tests for ONTO-03 serialization determinism and ontology tools/call reachability.
- Task 2 implements ontology MCP tool catalog/dispatch and deterministic response shaping per D-03.
- Task 3 aligns operator validation docs to executable ontology transport checks and concise outputs.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED ONTO-03 transport tests for ontology query and export snapshot</name>
  <read_first>.planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md, crates/turbo-mcp/tests/mcp_stdio_e2e.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</read_first>
  <files>crates/turbo-mcp/tests/ontology_mcp_e2e.rs</files>
  <behavior>
    - Test 1: `tools/list` advertises ontology query/export tool names.
    - Test 2: `tools/call` ontology export returns deterministic sorted entities/edges payload.
    - Test 3: repeated export with unchanged inputs yields byte-for-byte equivalent JSON serialization.
  </behavior>
  <action>Create failing subprocess MCP tests in `ontology_mcp_e2e.rs` that exercise initialize -> notifications/initialized -> tools/list -> tools/call for ontology query/export, asserting deterministic concise payload fields per D-03.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test ontology_mcp_e2e -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `crates/turbo-mcp/tests/ontology_mcp_e2e.rs` exists and references ontology tool names in `tools/call`.
    - First run of `cargo test -p turbo-mcp --test ontology_mcp_e2e -- --nocapture` fails in RED before Task 2.
  </acceptance_criteria>
  <done>Failing MCP transport tests define ONTO-03 deterministic serialization expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ontology MCP tool catalog and deterministic tools/call handlers</name>
  <read_first>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/tests/ontology_mcp_e2e.rs, crates/turbo-mcp/src/lib.rs</read_first>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</files>
  <behavior>
    - `tool_catalog` includes explicit ontology query/export tool names.
    - `tools/call` routes ontology operations to l3dg3rr-owned service abstractions (D-02), not upstream interface rewrites (D-01).
    - Success/error payloads remain concise, deterministic, and machine-readable for small-model agents (D-03).
  </behavior>
  <action>Add adapter argument parsing/result shaping and server dispatch arms for ontology query/export tools, reusing existing MCP passthrough/error-shaping patterns from Phase 13 and keeping local-first data access semantics (D-04).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test ontology_mcp_e2e -- --nocapture && cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp --test ontology_mcp_e2e -- --nocapture` passes.
    - `rg -n "ontology_.*(query|export)|tools/call" crates/turbo-mcp/src/mcp_adapter.rs crates/turbo-mcp/src/bin/turbo-mcp-server.rs` confirms executable MCP wiring.
  </acceptance_criteria>
  <done>Ontology query/export is callable via MCP with deterministic payload contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Align runbook and phase validation map to ontology MCP transport verification</name>
  <read_first>docs/agent-mcp-runbook.md, .planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md, crates/turbo-mcp/tests/ontology_mcp_e2e.rs</read_first>
  <files>docs/agent-mcp-runbook.md, .planning/phases/14-ontology-persistence-and-query-surface/14-VALIDATION.md</files>
  <action>Document ontology MCP tool usage and deterministic output expectations, then update 14-VALIDATION command mapping to real ONTO-03 test identifiers and quick/full test commands.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test ontology_contract -- --nocapture && cargo test -p turbo-mcp --test ontology_mcp_e2e -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `docs/agent-mcp-runbook.md` includes ontology query/export tool instructions.
    - `14-VALIDATION.md` maps ONTO-03 to concrete test commands with no `MISSING` entries.
    - Full turbo-mcp suite remains green.
  </acceptance_criteria>
  <done>Operator and verification artifacts align with executable ontology MCP behavior.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test ontology_mcp_e2e -- --nocapture`
- `cargo test -p turbo-mcp --test ontology_contract -- --nocapture`
- `cargo test -p turbo-mcp -- --nocapture`
</verification>

<acceptance_criteria>
- ONTO-03 ontology outputs are serialized in deterministic machine-readable form over MCP transport.
- MCP tool discovery and tools/call execution include ontology surfaces without regressing existing proxy tools.
- Validation and runbook artifacts match executable ontology transport checks.
</acceptance_criteria>

<success_criteria>
1. Small-model agents can query/export ontology state with concise deterministic payloads.
2. Transport-level tests prove ontology MCP surfaces function end-to-end.
3. Phase 14 scope stays within ontology persistence/query/serialization, without pulling reconciliation/HSM/event logic forward.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ontology-persistence-and-query-surface/14-02-SUMMARY.md`.
</output>
