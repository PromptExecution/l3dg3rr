---
phase: 13-mcp-boundary-and-agent-only-runtime-surface
plan: 03
type: execute
wave: 3
depends_on: ["13-mcp-boundary-and-agent-only-runtime-surface-02"]
files_modified:
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - crates/turbo-mcp/tests/mcp_adapter_contract.rs
  - crates/turbo-mcp/tests/mcp_stdio_e2e.rs
  - docs/agent-mcp-runbook.md
  - .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md
autonomous: true
gap_closure: true
requirements: ["DOC-01", "DOC-02", "DOC-03"]
must_haves:
  truths:
    - "The advertised rustledger proxy surface is callable through MCP tools/call."
    - "Rustledger proxy responses preserve deterministic canonical and provenance fields for small-model agent use."
    - "A transport-level subprocess test proves proxy_rustledger_ingest_statement_rows executes end-to-end."
  artifacts:
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "Executable tools/call dispatch branch for proxy_rustledger_ingest_statement_rows."
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Rustledger ingest-statement-rows argument parsing and deterministic response shaping."
    - path: "crates/turbo-mcp/tests/mcp_stdio_e2e.rs"
      provides: "Subprocess MCP verification that rustledger proxy tool call succeeds over transport."
  key_links:
    - from: "tools/list catalog entry proxy_rustledger_ingest_statement_rows"
      to: "tools/call handler branch"
      via: "server dispatch in turbo-mcp-server.rs"
      pattern: "proxy_rustledger_ingest_statement_rows"
    - from: "mcp_stdio_e2e rustledger proxy test"
      to: "adapter/service ingest_statement_rows path"
      via: "initialize -> notifications/initialized -> tools/call"
      pattern: "ingest_statement_rows|inserted_count|tx_ids"
---

<objective>
Close the verifier gap where `proxy_rustledger_ingest_statement_rows` is advertised but not executable via MCP transport.

Purpose: restore locked passthrough/proxy boundary integrity without inventing new upstream interfaces.
Output: callable rustledger proxy tool over MCP `tools/call`, plus transport-level proof and runbook/validation alignment.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-CONTEXT.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-RESEARCH.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VERIFICATION.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-02-SUMMARY.md
@crates/turbo-mcp/src/mcp_adapter.rs
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@crates/turbo-mcp/tests/mcp_adapter_contract.rs
@crates/turbo-mcp/tests/mcp_stdio_e2e.rs
@docs/agent-mcp-runbook.md

<decisions_locked>
- D-01: Do not reinvent upstream rustledger/docling MCP interfaces.
- D-02: Use passthrough/proxy model and keep l3dg3rr abstractions above upstream tools.
- D-03: Sandboxed agents must execute through l3dg3rr MCP tools/call only.
- D-04: Responses and status fields must remain deterministic and concise for small-model agents.
</decisions_locked>

<interfaces>
From crates/turbo-mcp/src/lib.rs:
```rust
pub struct IngestStatementRowsRequest {
    pub journal_path: PathBuf,
    pub workbook_path: PathBuf,
    pub rows: Vec<TransactionInput>,
}

pub struct IngestStatementRowsResponse {
    pub inserted_count: usize,
    pub tx_ids: Vec<String>,
}

pub trait TurboLedgerTools {
    fn ingest_statement_rows(
        &self,
        request: IngestStatementRowsRequest,
    ) -> Result<IngestStatementRowsResponse, ToolError>;
}
```

From crates/turbo-mcp/src/mcp_adapter.rs:
```rust
pub fn tool_catalog() -> Vec<String>;
pub fn normalize_rows_with_provenance(..., rows: Vec<TransactionInput>) -> Vec<Value>;
pub fn map_tool_error(error: &ToolError) -> Value;
pub fn unknown_tool_result(tool_name: &str) -> Value;
```
</interfaces>
</context>

<requirements_traceability>
- Task 1 maps failing verifier gap to executable MCP checks (DOC-01, DOC-03; D-03).
- Task 2 wires callable rustledger proxy behavior with deterministic shaping (DOC-01, DOC-02, DOC-03; D-01/D-02/D-04).
- Task 3 aligns operator/verifier documentation to real transport checks (DOC-01, DOC-02, DOC-03; D-03).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED transport tests for rustledger proxy callable surface</name>
  <read_first>.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VERIFICATION.md, crates/turbo-mcp/tests/mcp_adapter_contract.rs, crates/turbo-mcp/tests/mcp_stdio_e2e.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</read_first>
  <files>crates/turbo-mcp/tests/mcp_adapter_contract.rs, crates/turbo-mcp/tests/mcp_stdio_e2e.rs</files>
  <behavior>
    - `tools/list` includes `proxy_rustledger_ingest_statement_rows`.
    - `tools/call` to `proxy_rustledger_ingest_statement_rows` succeeds over stdio MCP transport.
    - Response includes deterministic `inserted_count`, stable `tx_ids`, and canonical/provenance fields.
  </behavior>
  <action>Create verifier-gap-focused tests that currently fail before wiring: add a subprocess MCP test calling `proxy_rustledger_ingest_statement_rows` and contract assertions tagged to DOC requirements per D-03.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_stdio_e2e rustledger_proxy -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "proxy_rustledger_ingest_statement_rows" crates/turbo-mcp/tests/mcp_stdio_e2e.rs crates/turbo-mcp/tests/mcp_adapter_contract.rs` shows explicit coverage.
    - `cargo test -p turbo-mcp --test mcp_stdio_e2e rustledger_proxy -- --nocapture` fails in RED before Task 2 implementation.
  </acceptance_criteria>
  <done>A failing transport test captures the exact gap (advertised tool not callable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire rustledger proxy handler in adapter and stdio tools/call dispatch</name>
  <read_first>crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/tests/mcp_stdio_e2e.rs</read_first>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs</files>
  <behavior>
    - `proxy_rustledger_ingest_statement_rows` arguments parse into `IngestStatementRowsRequest` and enforce input-shape invariants.
    - `tools/call` routes this tool to `TurboLedgerTools::ingest_statement_rows` (no bypass, per D-03).
    - Success response returns deterministic canonical/provenance fields with `provider: "rustledger"` and `backend_tool: "ingest_statement_rows"` per D-01/D-02.
    - Error responses use existing deterministic `isError`/`error_type` mapping per D-04.
  </behavior>
  <action>Add adapter helpers for rustledger row-ingest parsing/result shaping and connect a new `tools/call` match arm in `turbo-mcp-server.rs`; keep passthrough/proxy semantics and do not introduce new upstream interface inventions (D-01).</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture && cargo test -p turbo-mcp --test mcp_stdio_e2e rustledger_proxy -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture` passes.
    - `cargo test -p turbo-mcp --test mcp_stdio_e2e rustledger_proxy -- --nocapture` passes.
    - `rg -n "proxy_rustledger_ingest_statement_rows" crates/turbo-mcp/src/bin/turbo-mcp-server.rs crates/turbo-mcp/src/mcp_adapter.rs` confirms executable dispatch and shaping code.
  </acceptance_criteria>
  <done>The rustledger proxy surface is callable and deterministic through MCP transport.</done>
</task>

<task type="auto">
  <name>Task 3: Align runbook and validation map to rustledger proxy transport verification</name>
  <read_first>docs/agent-mcp-runbook.md, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md, crates/turbo-mcp/tests/mcp_stdio_e2e.rs, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VERIFICATION.md</read_first>
  <files>docs/agent-mcp-runbook.md, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md, crates/turbo-mcp/tests/mcp_stdio_e2e.rs</files>
  <action>Update runbook and validation mapping to include the new rustledger proxy transport test command and expected outputs, ensuring docs no longer claim an uncallable surface and explicitly reference MCP-only execution per D-03.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture && bash scripts/mcp_e2e.sh && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "proxy_rustledger_ingest_statement_rows|tools/call|MCP-only" docs/agent-mcp-runbook.md` returns updated guidance.
    - `.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md` references rustledger proxy transport checks by real command/test name.
    - Full verification commands pass.
  </acceptance_criteria>
  <done>Validation and operator docs match executable MCP behavior for both docling and rustledger proxy tools.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture`
- `cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture`
- `bash scripts/mcp_e2e.sh`
- `cargo test -p turbo-mcp -- --nocapture`
</verification>

<acceptance_criteria>
- The verifier gap in `13-VERIFICATION.md` is closed: `proxy_rustledger_ingest_statement_rows` is callable via MCP `tools/call`.
- Transport-level tests demonstrate deterministic result payloads for rustledger proxy ingest.
- Runbook and validation artifacts no longer describe unimplemented rustledger callable behavior.
</acceptance_criteria>

<success_criteria>
1. MCP catalog and MCP execution path for rustledger proxy are fully connected.
2. Gap closure keeps locked passthrough/proxy decision and avoids reinventing upstream interfaces.
3. DOC-01/02/03 transport verification remains green with added rustledger proxy coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-03-SUMMARY.md`.
</output>
