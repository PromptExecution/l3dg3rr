---
phase: 13-mcp-boundary-and-agent-only-runtime-surface
plan: 01
type: execute
wave: 1
depends_on: ["06-local-deployment-release-readiness"]
files_modified:
  - crates/turbo-mcp/Cargo.toml
  - crates/turbo-mcp/src/lib.rs
  - crates/turbo-mcp/src/mcp_adapter.rs
  - crates/turbo-mcp/src/bin/turbo-mcp-server.rs
  - crates/turbo-mcp/tests/mcp_adapter_contract.rs
autonomous: true
requirements: ["DOC-01", "DOC-02"]
must_haves:
  truths:
    - "Agents can reach ingestion capabilities through a real MCP transport surface instead of direct Rust calls."
    - "l3dg3rr MCP responses expose deterministic canonical transaction fields and provenance metadata."
    - "A concise deterministic status tool reports upstream backend readiness and blocking preconditions."
  artifacts:
    - path: "crates/turbo-mcp/src/bin/turbo-mcp-server.rs"
      provides: "stdio MCP transport entrypoint and lifecycle boundary."
    - path: "crates/turbo-mcp/src/mcp_adapter.rs"
      provides: "Proxy/passthrough tool mapping for rustledger/docling plus l3dg3rr abstractions."
    - path: "crates/turbo-mcp/tests/mcp_adapter_contract.rs"
      provides: "Requirement-tagged contract tests for deterministic MCP schemas and status output."
  key_links:
    - from: "turbo-mcp stdio server"
      to: "mcp_adapter tool handlers"
      via: "MCP initialize/tools/list/tools/call lifecycle"
      pattern: "initialize|tools/list|tools/call"
    - from: "mcp_adapter passthrough handlers"
      to: "TurboLedgerService methods"
      via: "provider-tagged proxy mapping (rustledger/docling) per locked decision"
      pattern: "provider|backend_tool|backend_version|backend_call_id"
---

<objective>
Establish the enforceable MCP transport/proxy boundary for Phase 13.

Purpose: satisfy the locked decision that l3dg3rr must passthrough/proxy upstream MCP interfaces (rustledger + docling) and only add higher-level abstractions above them.
Output: a runnable stdio MCP server with deterministic schemas/status hints and contract tests proving DOC-01/DOC-02 surface readiness.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-CONTEXT.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-RESEARCH.md
@crates/turbo-mcp/src/lib.rs
@crates/turbo-mcp/tests/interface.rs

<decisions_locked>
- D-01: Do not reinvent upstream MCP servers; l3dg3rr must proxy/passthrough rustledger/docling interfaces.
- D-02: l3dg3rr owns higher-level normalization/orchestration abstractions above passthrough providers.
- D-03: Sandboxed agents must use l3dg3rr MCP capabilities only.
- D-04: Responses must be deterministic and concise for small-model agents.
</decisions_locked>

<interfaces>
From `crates/turbo-mcp/src/lib.rs`:
```rust
pub struct IngestPdfRequest {
    pub pdf_path: String,
    pub journal_path: PathBuf,
    pub workbook_path: PathBuf,
    pub raw_context_bytes: Option<Vec<u8>>,
    pub extracted_rows: Vec<TransactionInput>,
}

pub struct IngestPdfResponse {
    pub inserted_count: usize,
    pub tx_ids: Vec<String>,
}

pub trait TurboLedgerTools {
    fn ingest_pdf(&self, request: IngestPdfRequest) -> Result<IngestPdfResponse, ToolError>;
    fn ingest_statement_rows(&self, request: IngestStatementRowsRequest)
        -> Result<IngestStatementRowsResponse, ToolError>;
    fn get_raw_context(&self, request: GetRawContextRequest)
        -> Result<GetRawContextResponse, ToolError>;
}
```
</interfaces>
</context>

<task_checklist>
- [ ] Task 1 (no dependencies): Add failing contract tests for MCP boundary/proxy surface and deterministic status payload.
- [ ] Task 2 (depends on Task 1): Implement stdio server + MCP adapter proxy/passthrough wiring with provider provenance metadata.
- [ ] Task 3 (depends on Task 2): Normalize deterministic error/status outputs and pass full turbo-mcp contract suite.
</task_checklist>

<requirements_traceability>
- Task 1 defines failing contract tests for DOC-01 and DOC-02 (per D-01/D-03/D-04).
- Task 2 implements MCP transport boundary and passthrough/proxy abstractions for DOC-01 (per D-01/D-02/D-03).
- Task 3 finalizes deterministic canonical response and status/error shaping for DOC-02 (per D-04).
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define MCP boundary/proxy contracts as failing tests</name>
  <read_first>.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-CONTEXT.md, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-RESEARCH.md, crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/tests/interface.rs</read_first>
  <files>crates/turbo-mcp/tests/mcp_adapter_contract.rs</files>
  <behavior>
    - Test 1 (DOC-01, D-01/D-03): stdio MCP server exposes tool catalog for provider passthrough/proxy operations and blocks direct in-process-only path assumptions.
    - Test 2 (DOC-02, D-02/D-04): tool outputs contain deterministic canonical fields (`account,date,amount,description,currency,source_ref`) and provenance (`provider,backend_tool,backend_version,backend_call_id` where available).
    - Test 3 (DOC-02, D-04): `get_pipeline_status` style tool returns stable enum-like status + blockers + concise next_hint.
  </behavior>
  <action>Create RED-phase contract tests that exercise MCP adapter expectations without implementing handlers yet; include requirement tags in test names/comments.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DOC-01|DOC-02" crates/turbo-mcp/tests/mcp_adapter_contract.rs` returns requirement-tagged coverage.
    - `cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture` fails due to missing adapter/server implementation (RED phase).
    - Tests assert explicit provider/provenance keys and deterministic status keys.
  </acceptance_criteria>
  <done>Failing tests unambiguously define MCP boundary, passthrough/proxy metadata, and deterministic status behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stdio MCP server and passthrough/proxy adapter layer</name>
  <read_first>crates/turbo-mcp/tests/mcp_adapter_contract.rs, crates/turbo-mcp/src/lib.rs, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-RESEARCH.md</read_first>
  <files>crates/turbo-mcp/Cargo.toml, crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/src/lib.rs</files>
  <behavior>
    - MCP stdio transport server performs lifecycle-compliant tool serving for sandboxed agents (D-03).
    - Adapter implements passthrough/proxy semantics to upstream rustledger/docling interfaces (D-01), with higher-level orchestration wrapper in l3dg3rr (D-02).
    - Tool payloads include deterministic canonical fields and provider provenance metadata.
  </behavior>
  <action>Create an RMCP-based adapter module and server binary; map tool calls to existing `TurboLedgerService` capabilities and upstream provider proxies, explicitly avoiding reinvention of upstream MCP primitives per D-01.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture` passes.
    - `rg -n "provider|backend_tool|backend_version|backend_call_id" crates/turbo-mcp/src/mcp_adapter.rs` confirms provenance fields.
    - `rg -n "serve|stdin|stdout|tools/list|tools/call" crates/turbo-mcp/src/bin/turbo-mcp-server.rs crates/turbo-mcp/src/mcp_adapter.rs` confirms real transport boundary.
  </acceptance_criteria>
  <done>Transport boundary is runnable and adapter contracts pass with passthrough/proxy semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize deterministic status/error shaping and full turbo-mcp verification</name>
  <read_first>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/tests/mcp_adapter_contract.rs, crates/turbo-mcp/tests/interface.rs</read_first>
  <files>crates/turbo-mcp/src/mcp_adapter.rs, crates/turbo-mcp/src/lib.rs, crates/turbo-mcp/tests/mcp_adapter_contract.rs</files>
  <behavior>
    - Status output remains concise and deterministic with stable field names and enum-like values (D-04).
    - Adapter error mapping separates protocol errors from domain execution errors with consistent `isError` semantics.
    - Existing direct-contract tests remain green while MCP adapter contracts stay green.
  </behavior>
  <action>Harden error/status normalization and remove any non-deterministic output wording so repeated calls produce stable shapes for small-model agents.</action>
  <verify>
    <automated>cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp -- --nocapture` passes.
    - `rg -n "get_pipeline_status|status|blockers|next_hint" crates/turbo-mcp/src/mcp_adapter.rs` confirms deterministic status contract.
    - `rg -n "isError|InvalidInput|Internal" crates/turbo-mcp/src/mcp_adapter.rs` confirms explicit error mapping branches.
  </acceptance_criteria>
  <done>turbo-mcp test suite passes with stable MCP boundary, status, and error semantics.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test mcp_adapter_contract -- --nocapture` passes.
- `cargo test -p turbo-mcp -- --nocapture` passes.
- Adapter/service boundary preserves deterministic canonical mapping and provenance fields per DOC-02.
</verification>

<success_criteria>
1. Agents can run l3dg3rr via stdio MCP transport and discover/call tools without direct Rust API invocation.
2. Passthrough/proxy model to rustledger/docling is explicit in adapter contracts and metadata fields.
3. Deterministic status/error outputs are concise and stable for small-model agents.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-01-SUMMARY.md`.
</output>
