---
phase: 13-mcp-boundary-and-agent-only-runtime-surface
plan: 02
type: execute
wave: 2
depends_on: ["13-mcp-boundary-and-agent-only-runtime-surface-01"]
files_modified:
  - crates/turbo-mcp/tests/mcp_stdio_e2e.rs
  - scripts/mcp_e2e.sh
  - docs/agent-mcp-runbook.md
  - .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md
autonomous: true
requirements: ["DOC-01", "DOC-02", "DOC-03"]
must_haves:
  truths:
    - "DOC-01 ingestion can be executed end-to-end through MCP tool calls only."
    - "DOC-02 canonical mapping is deterministic and validated through transport-level assertions."
    - "DOC-03 replaying the same source through MCP returns stable IDs with no duplicate insertions."
  artifacts:
    - path: "crates/turbo-mcp/tests/mcp_stdio_e2e.rs"
      provides: "Subprocess MCP-only validation for DOC-01/02/03."
    - path: "scripts/mcp_e2e.sh"
      provides: "Single-command reproducible MCP bootstrap and test run."
    - path: "docs/agent-mcp-runbook.md"
      provides: "Agent/operator runbook for MCP-only setup, tool discovery, and troubleshooting."
  key_links:
    - from: "mcp_stdio_e2e test harness"
      to: "turbo-mcp-server subprocess"
      via: "initialize -> notifications/initialized -> tools/list -> tools/call"
      pattern: "initialize|notifications/initialized|tools/list|tools/call"
    - from: "ingest tool replay path"
      to: "deterministic tx id and inserted_count"
      via: "repeat same source input over MCP"
      pattern: "inserted_count|tx_ids"
---

<objective>
Prove DOC-01/DOC-02/DOC-03 using MCP-only execution and document the operator workflow.

Purpose: close the milestone blocker by demonstrating that ingestion behavior is verifiably correct through transport-only calls, not direct in-process service usage.
Output: passing MCP stdio e2e tests and runbook documentation for agent-only operation.
</objective>

<execution_context>
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/workflows/execute-plan.md
@/home/brianh/promptexecution/mbse/l3dg3rr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-CONTEXT.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-RESEARCH.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md
@.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-01-PLAN.md
@crates/turbo-mcp/src/bin/turbo-mcp-server.rs
@crates/turbo-mcp/src/mcp_adapter.rs

<decisions_locked>
- D-01: Do not reinvent upstream MCP servers; use passthrough/proxy over rustledger/docling.
- D-02: l3dg3rr adds higher-level abstractions over upstream interfaces.
- D-03: Sandboxed workflows must execute through l3dg3rr MCP interface only.
- D-04: Outputs/status are deterministic and concise for small models.
</decisions_locked>
</context>

<task_checklist>
- [ ] Task 1 (no dependencies): Define RED-phase transport-only DOC tests for ingest, canonical mapping, and replay idempotency.
- [ ] Task 2 (depends on Task 1): Implement subprocess MCP harness and shell wrapper to satisfy all DOC tests without direct service calls.
- [ ] Task 3 (depends on Task 2): Publish MCP-only operator runbook and align validation map to new commands.
</task_checklist>

<requirements_traceability>
- Task 1 defines failing tests for DOC-01, DOC-02, DOC-03 through MCP-only flow (D-03).
- Task 2 implements passing MCP subprocess verification for DOC-01, DOC-02, DOC-03 (D-01/D-02/D-03/D-04).
- Task 3 documents bootstrap/discovery/replay verification flow for DOC-01, DOC-02, DOC-03.
</requirements_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define MCP-only DOC requirement tests as failing subprocess scenarios</name>
  <read_first>.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/src/mcp_adapter.rs</read_first>
  <files>crates/turbo-mcp/tests/mcp_stdio_e2e.rs</files>
  <behavior>
    - DOC-01: test ingests statement candidates via MCP `tools/call` only and validates provenance fields.
    - DOC-02: test checks deterministic canonical mapping fields from MCP response payloads.
    - DOC-03: test replays same source through MCP and asserts stable tx IDs + `inserted_count` idempotency.
  </behavior>
  <action>Create RED-phase subprocess tests that spawn `turbo-mcp-server`, perform protocol lifecycle handshake, and prohibit direct `TurboLedgerService` invocation in the e2e file.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DOC-01|DOC-02|DOC-03" crates/turbo-mcp/tests/mcp_stdio_e2e.rs` confirms requirement-tagged coverage.
    - `rg -n "TurboLedgerService::from_manifest_str" crates/turbo-mcp/tests/mcp_stdio_e2e.rs` returns no matches.
    - `cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture` fails for expected RED-phase gaps.
  </acceptance_criteria>
  <done>Failing MCP-only e2e tests exist and explicitly codify DOC requirement outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MCP subprocess harness and deterministic replay assertions</name>
  <read_first>crates/turbo-mcp/tests/mcp_stdio_e2e.rs, crates/turbo-mcp/src/bin/turbo-mcp-server.rs, crates/turbo-mcp/src/mcp_adapter.rs, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-RESEARCH.md</read_first>
  <files>crates/turbo-mcp/tests/mcp_stdio_e2e.rs, scripts/mcp_e2e.sh</files>
  <behavior>
    - Test harness performs strict lifecycle: initialize, initialized notification, tools/list, tools/call.
    - DOC-01/02 assertions inspect deterministic canonical + provenance fields from tool responses.
    - DOC-03 assertions replay identical input and verify stable `tx_ids` with zero duplicate inserts.
  </behavior>
  <action>Implement reusable JSON-RPC stdio client helpers in the test file and a script wrapper that executes the same e2e suite; keep all calls transport-level and avoid service-level bypass paths per D-03.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture` passes.
    - `bash scripts/mcp_e2e.sh` exits 0.
    - `rg -n "initialize|tools/list|tools/call" crates/turbo-mcp/tests/mcp_stdio_e2e.rs` confirms full MCP lifecycle coverage.
  </acceptance_criteria>
  <done>MCP-only subprocess e2e suite passes and demonstrates deterministic replay safety.</done>
</task>

<task type="auto">
  <name>Task 3: Publish MCP-only runbook and update validation contract</name>
  <read_first>.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md, crates/turbo-mcp/tests/mcp_stdio_e2e.rs, scripts/mcp_e2e.sh, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-CONTEXT.md</read_first>
  <files>docs/agent-mcp-runbook.md, .planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md</files>
  <behavior>
    - Runbook documents MCP-only bootstrap, tool discovery, and deterministic troubleshooting steps.
    - Validation map commands point at MCP stdio e2e test names covering DOC-01/02/03.
    - Documentation explicitly states passthrough/proxy model to rustledger/docling per locked decision.
  </behavior>
  <action>Write a concise operator/agent runbook with exact commands and expected outputs, then update Phase 13 validation table to match real test names/commands produced in Task 2.</action>
  <verify>
    <automated>cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture && cargo test -p turbo-mcp -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "passthrough|proxy|rustledger|docling|tools/list|tools/call" docs/agent-mcp-runbook.md` returns expected guidance.
    - `.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-VALIDATION.md` task map references real e2e test identifiers/commands.
    - Both verification commands pass.
  </acceptance_criteria>
  <done>Phase 13 has executable MCP-only docs and aligned validation artifacts for downstream verify-work.</done>
</task>

</tasks>

<verification>
- `cargo test -p turbo-mcp --test mcp_stdio_e2e -- --nocapture` passes with DOC-01/02/03 assertions.
- `bash scripts/mcp_e2e.sh` passes from repo root.
- Runbook commands match implementation and validation map entries.
</verification>

<success_criteria>
1. DOC-01 ingestion path is verified through MCP tool calls only.
2. DOC-02 canonical mapping fields are deterministic and transport-verified.
3. DOC-03 replay idempotency is transport-verified with stable IDs and no duplicates.
4. Operators and agents can follow one runbook for MCP-only bootstrap and troubleshooting.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mcp-boundary-and-agent-only-runtime-surface/13-02-SUMMARY.md`.
</output>
